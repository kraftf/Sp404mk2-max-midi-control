# SP-404 MK2 Control Patch — Session State (Control-35)
Date: 2026-07-13. Roland_SP404MK2_Control-31.maxpat (563 objects, 903 lines) remains the
last hardware-verified build. Control-32 (703 objects, 1135 lines) replaced its preset
system with pattrstorage/pattrforward and passed all no-hardware Max tests. Control-33
(716 objects, 1159 lines) fixed the four never-compiling SYNC-detect expr ternaries,
added the 5 DFX slot assignments to the preset system, and fixed two Max-test-found bugs
(cross-group bus-switch label scramble; post-SAVE/RECALL MIDI-channel-5 residue) — ALL
FOUR retested and confirmed working by the user in Max (2026-07-12, no hardware).
Roland_SP404MK2_Control-34.maxpat (790 boxes, 1258 lines) builds on Control-33: adds
MIDI Control Input (external CC control of the 8 hardware-facing controls) and expands
the preset system to 128 slots (full PC range) with per-preset renaming — see
"Control-34" section below.
User-tested in Max (2026-07-12): MIDI Control Input confirmed working first try. Preset
numbering, SAVE, RECALL, and per-preset renaming (single-word and multi-word) are ALL
now CONFIRMED WORKING by the user, including the always-128-slots display and the
"symbol"-stripping fix. Getting there took nine rounds total across this session: a
from-scratch coll/uzi/sprintf/zl.join system (six rounds, each fixing one bug, twice
regressing or exposing another); a full rewrite onto pattrstorage's own native
slot-naming protocol after the user supplied a working reference patch of theirs
(`getslotnamelist`/`slotname <n> <name>`/`slotname done`) that fixed renaming but exposed
a genuine architecture conflict (the menu got permanently stuck at however many slots had
been saved); a further rewrite, per the user's explicit choice, to a two-phase CAPTURE
(write pattrstorage's replies into a coll) then REBUILD (unconditionally show all 128
slots, falling back to a default for any never-used slot) design; and a final fix
stripping `coll`'s "symbol <value>" wrapping from single-word renames. See "Retest: works,
but single-word renames show a literal 'symbol' prefix" below (and the sections above it)
for the full blow-by-blow if this needs revisiting.

Roland_SP404MK2_Control-35.maxpat (830 boxes, 1333 lines) builds on Control-34: adds a
dedicated Program Change input port and a second (coll-based, not pattrstorage-based)
preset system for the 8 MIDI CC selectors, and investigates a user-reported performance
problem. See "Control-35" section below. First real-Max test found two bugs (RECALL
crashing with a console error; rename silently failing for multi-word names) — both
fixed same session and CONFIRMED WORKING by the user ("Ok it works.") on retest. Same
message also asked for two follow-ups, both done same session: the CC-mapping presets
now also save/recall the MIDI Control Input Device (by name, not umenu index, since MIDI
ports can be reordered/reconnected between sessions); and the Program Change Input Device
selector was repositioned in presentation next to the bus-state preset controls. See
"Control-35" section below for both.
NOT YET tested: the device-name save/recall addition and the presentation reposition have
not been opened in Max yet. Real SP-404 MK2 hardware also remains untested throughout
(per-bus MIDI channel dispatch, PC 0-127 recall to the actual unit).

## 35: Dedicated PC input port + CC-mapping preset system + performance investigation
Generated by `sp404gen/build_control35.py` (from Control-34); structurally validated by
`sp404gen/validate_control35.py`. Confirmed via diff: 0 boxes/lines removed, only the 2
expected presets-filename text changes to existing boxes, 35 boxes / 65 lines added.

### 1. Dedicated Program Change input port
`obj-pv2-pgmin` (drives preset recall) was a bare `pgmin` with NO incoming connection at
all — confirmed by checking the patch directly — meaning it listened to Program Change
on every connected MIDI device/port with no filtering whatsoever. Asked the user whether
this should share the existing "MIDI Control Input Device" selector (obj-c34-indev, used
for CC control) or get its own independent one; they chose independent. Added
obj-c35-pcindev (umenu, item list cloned from the same MIDI Output Device list as
everywhere else in this patch) -> obj-c35-pc-pport (`prepend port`) -> obj-pv2-pgmin
inlet 0. This is the exact same device-selector idiom already confirmed working for
obj-c34-ctlin in Control-34 (user tested, "confirmed working first try") — same
mechanism, different MIDI message type, so treated as low-risk by direct analogy rather
than needing independent verification of the underlying "port" message behavior.

### 2. CC-mapping preset system (16 slots) — built WITHOUT a second pattrstorage
User asked for "a second pattrstorage with the exact same mechanism" to save/recall/
rename different MIDI controllers' CC-number configurations (the 8 selectors added in
Control-34: ctrl1-6, efxsel, onoff). Investigated whether a second pattrstorage is safe
first, since getting this wrong risks silently corrupting the already-hard-won 128-preset
system. Found: obj-pv2-pattrstorage runs `@subscribemode 1`, which auto-subscribes EVERY
parameter-enabled object anywhere in this (fully flat, non-subpatched) patcher — verified
empirically (52 parameter_enable=1 objects across all 4 buses/EFX are its clients,
confirmed via a JSON scan, not by assumption). The 8 CC selector number boxes were
deliberately left with parameter_enable UNSET in Control-34 specifically so MIDI CC input
could never leak into the 128-preset system. Turning parameter_enable on for them (which
would be needed to register them as clients of a NEW pattrstorage) would ALSO make the
EXISTING pattrstorage's subscribemode auto-discovery adopt them, silently pulling MIDI-
controller CC numbers into every one of the 128 hardware presets — exactly the
cross-contamination Control-34 went out of its way to prevent. There is no documented,
low-risk way to scope a second pattrstorage's clients to a hand-picked subset without
either subscribemode's global auto-scan or explicit `pattr` proxy objects (which risk
being auto-adopted by the FIRST pattrstorage's own subscribemode scan too, since it scans
the whole patcher, not just objects that existed when it was built).

Given this, built the CC-mapping preset system on two explicit embedded colls instead —
obj-c35-ccvalues (8 CC numbers per slot) and obj-c35-ccnames (label per slot), both keyed
1-16, both pre-seeded so lookups always find something. This delivers the identical
user-facing mechanism (umenu of 16 slots, SAVE, RECALL, rename with the same
textedit/keymode/outputmode/route-text recipe proven in Control-34) with zero interaction
with pattrstorage's parameter system — confirmed via JSON scan that the parameter_enable=1
count in Control-35 is still exactly 52, unchanged from Control-34.
- SAVE: `obj-c35-save-t` (`t b b b b b b b b b`, 9 outlets) bangs each of the 8 ccsel
  number boxes (outlets 1-8, order matches CC_TARGETS = ctrl1..6, efxsel, onoff) into
  `obj-c35-savepack` (`pack 0 0 0 0 0 0 0 0 0`) cold inlets 1-8 in that same order, then
  outlet 0 (fires LAST, guaranteeing all 8 cold inlets are already filled) fetches the
  current slot via a dedicated shadow int + `+1` into savepack's HOT inlet 0, writing
  `[slot v_ctrl1 v_ctrl2 ... v_onoff]` into obj-c35-ccvalues.
  ⚠ Caught and fixed during self-review (before any Max testing): an earlier draft used
  reversed outlet/inlet indices (`8 - i`) for the bang-to-ccsel and ccsel-to-pack wiring.
  The ordering requirement is ONLY "all 8 cold inlets fire before the hot inlet" (satisfied
  by `t`'s right-to-left firing regardless of which specific outlet drives which target) —
  it does NOT require any particular outlet-to-target assignment. The reversed version
  would have written coll records in [onoff..ctrl1] order while RECALL's `unpack` reads
  them in [ctrl1..onoff] order, silently swapping every CC value between controls on
  recall. Fixed to a natural `i + 1` mapping on both outlet and pack-inlet index, which
  now matches unpack's outlet order directly. Caught by manually tracing the atom order
  before ever generating the patch, not by Max testing — flagging in case the underlying
  reasoning needs re-deriving later.
- RECALL: fetch slot -> lookup obj-c35-ccvalues -> `unpack 0 0 0 0 0 0 0 0` -> outlet i
  dispatches directly to ccsel(CC_TARGETS[i])'s inlet 0 (sets + outputs, same as a live
  edit, so it also drives the existing `==`/gate wiring from Control-34 unchanged).
- Rename: textedit -> route text -> pack with the continuously-tracked current slot ->
  write directly into obj-c35-ccnames (no external async protocol involved, unlike
  pattrstorage's getslotnamelist/slotname round trip in Control-34) -> immediately
  rebuild the menu in the same trigger cascade (clear + `uzi 16` + append "N - name",
  reading from obj-c35-ccnames, `route symbol` stripping the same "symbol <value>"
  wrapping risk as namecache). No CAPTURE phase needed since there is no external object
  to wait on — this coll is the sole, synchronous source of truth, which is the one
  place this system is genuinely simpler than the 128-preset one.

### 3. Performance investigation (edit-mode sluggish, presentation-mode memory 40GB+)
Investigated structurally — this session cannot run Max, so no profiling was possible.
Checked and ruled out: feedback loops through pattrstorage/slotmenu (traced every
connection to/from both; confirmed umenu does not fire its outlet on `append`/`clear`/
`set`, so the rebuild routines cannot retrigger themselves), runaway loadbangs (7 total,
all one-shot, no duplication), pathological coordinates (patching_rect spans a plausible
40-6365 x / 10-5272 y, nothing absurd), oversized embedded data (main patch JSON is
642KB, unremarkable), and stray/dead objects (none found beyond what Control-34 already
cleaned up).
What IS true: the patch is 790 boxes / 1258 lines with ZERO subpatchers — completely
flat — including 46 live.dial/live.menu/live.toggle objects and 52 parameter-enabled
(pattr) objects, all pre-dating this session. That combination (large flat canvas + many
Live-family UI objects + a large pattr/parameter dictionary) is a well-documented source
of exactly this kind of Max editor sluggishness and presentation-mode memory/redraw cost
in the wider Max community, though this could not be confirmed as THE root cause without
being able to reproduce it.
Deliberately did NOT restructure the existing Control-33/34 baseline (subpatching it, or
changing pattrstorage's @subscribemode setting) to try to fix this blind — that baseline
took nine rounds of real-Max-testing to get right, and a structural change with no way to
test it carries a real risk of silent regression across the whole patch, not just one
feature. Both NEW features in this build were kept flat and built following the existing
codebase's own conventions, so they add to the object count but introduce no new
subpatcher/send-receive risk surface. If the user reports back that sluggishness/memory
usage is still a problem after testing Control-35, the recommended next step is a
DEDICATED, separately-scoped follow-up: encapsulating the pre-existing baseline UI (the
multiple bus mixer strips, DFX sections) into subpatchers, tested incrementally one
section at a time in real Max rather than attempted in one blind pass.

### ⚠ MAX-TEST ITEMS (Control-35)
a) Dedicated PC input port: does sending `port N` to a bare `pgmin`'s left inlet actually
   restrict it the same way it does for `ctlin` (confirmed working in Control-34)? This
   session assumed the "port" message behavior is shared across all MIDI-in object types
   (ctlin/pgmin/notein etc.) by direct analogy; never independently verified for pgmin
   specifically. Still untested (unrelated to the two bugs found/fixed on retest).
b) CC-mapping preset system core (SAVE/RECALL of the 8 CC numbers, rename): **CONFIRMED
   WORKING by the user** after the RECALL and rename fixes above.
c) CC-mapping preset system's NEW device-name save/recall, and the PC Input Device
   reposition: NOT yet opened in Max at all — see the two new subsections above for what
   to check on retest.
d) Performance: still no way to verify whether anything in this build measurably helps
   (or hurts) without the user testing directly.

### First Max test found two real bugs — both fixed same session
User's first real-Max test of Control-35 reported: (1) RECALL prints console error
`int: doesn't understand "RECALL"` and does nothing; (2) the rename box doesn't work at
all.

**Bug 1 — RECALL wired straight into an `int` object.** `obj-c35-ccrclbtn` (a message
box, text "RECALL") was wired directly into `obj-c35-ccshadow-rcl` (`int 0`) inlet 0. `t`
(trigger) objects fire their configured outlet types regardless of what triggered them,
which is why SAVE's identical-shaped click (message box "SAVE" -> `obj-c35-save-t`, a `t`
object) worked fine — but `int` is not a trigger; it only understands bang/int/float, and
a bare symbol like "RECALL" lands as exactly the reported console error. Fixed: inserted
`obj-c35-rcl-t` (`t b`) between the RECALL button and the shadow int, so RECALL now goes
through a trigger first, exactly mirroring SAVE's already-correct path.

**Bug 2 — rename used `pack s i`, which silently clobbers the slot number for any
multi-word name.** Confirmed via Cycling '74's own `pack` documentation: when a
multi-atom list arrives at one inlet, "the first item is stored in the location that
corresponds to the inlet in which it was received, and each subsequent item is stored as
if it had arrived in subsequent inlets" — and a symbol landing in a number-typed inlet is
converted to 0. `obj-c35-ccnamepack` (`pack s i`, hot=typed name, cold=slot#) would take a
2+-word name like "My Kit", store "My" in its own inlet, and spill "Kit" into the NEXT
inlet — the int-typed slot number — zeroing it. The rename would then silently land on
coll key 0 (outside the displayed 1-16 range, invisible) while the intended slot's name
never changed, exactly matching "the rename box doesn't work". This is the identical
class of multi-atom-truncation problem `zl.join` was already introduced to solve
elsewhere in this codebase (the Control-34 name cache) — replaced `pack s i` +
a `"$2 $1"` reordering message with `zl.join` (hot inlet = freshly-fetched slot number,
fires last and becomes the first segment of the output; cold inlet = the name, stored
first) so the coll write is always exactly `[slot, name...]` regardless of word count.
This also removed the now-unnecessary `obj-c35-ccnamemsg` message box and the
continuously-tracked `obj-c35-ccslot1` (replaced by a dedicated bang-fetched shadow,
`obj-c35-ccshadow-rename`, matching the SAVE/RECALL shadow idiom already used elsewhere
in this same feature).
⚠ **Open concern raised to the user, still unanswered:** Control-34's own rename
mechanism (`obj-c34-namepack`, also `pack s i`) has the exact same shape and is likely
subject to the identical multi-word-clobbering bug, despite having been reported
"confirmed working" for multi-word names earlier this session. Not touched without the
user's explicit go-ahead, given how many rounds it took to get that system stable. The
user's next message ("Ok it works.") confirmed the two Control-35 fixes but did not
address this question — still open, revisit if the user reports a 128-preset multi-word
rename problem, or ask again directly.
**CONFIRMED WORKING by the user** ("Ok it works.") — both fixes retested successfully.

### CC-mapping presets now also save/recall the MIDI Control Input Device, by name
User's next request (same message as the "Ok it works." confirmation): the CC-mapping
preset system should also remember which MIDI device (port) obj-c34-indev was set to,
since the whole point of that preset system is different physical MIDI controllers, and
switching controllers usually means switching which port carries their CC signals too.
Explicit caution from the user: MIDI ports can be reordered/reconnected/renamed between
sessions, so the umenu's raw index should NOT be what's saved -- the device's NAME should
be, with RECALL re-selecting by matching text rather than replaying a stale index that
might now point at a different device.
Researched umenu's actual outlet/message behavior via Cycling '74's own docs before
building (this exact class of "trust docs, not assumption" was the fix for both bugs
above): outlet 0 = selected index, outlet 1 = selected item's TEXT as a message, outlet 2
= status; bang re-outputs both outlet 0 and outlet 1 with the CURRENT selection (confirmed
explicitly, not assumed); "symbol <name>" sent to inlet 0 selects an item by matching text
AND fires normal output (unlike "setsymbol", which is silent) -- exactly what's needed so
RECALL's device selection also re-triggers the real port change (obj-c34-in-pport ->
ctlin), not just a silent display update.
Also confirmed (by reading this patch's own obj-4 device list directly) that MIDI device
names are frequently multi-word (e.g. "Arturia BeatStep Pro Arturia BeatStepPro",
"iConnectMIDI4+ PYRA A - NFUP") -- used `zl.join`, not `pack`, for combining the device
name with the slot number, for the identical reason `zl.join` replaced `pack s i` in the
rename fix above (pack's multi-atom spillover into the next inlet). This was a purely
proactive/defensive choice this time, not a bug found by testing.
Third embedded coll added: obj-c35-ccdevice (16 slots, keyed 1-16, default `['(unset)']`
-- a sentinel not expected to match any real device name, so recalling a never-saved slot
is a harmless no-op).
- SAVE: extended obj-c35-save-t from 9 to 10 outlets. New outlet 9 (fires FIRST, before
  everything else) bangs obj-c34-indev, causing it to re-output its current selection;
  outlet 1 (the device name text) feeds a new obj-c35-savedev-zljoin's cold inlet. The
  existing save-slotplus1 (slot number) now fans out to BOTH obj-c35-savepack's hot inlet
  (unchanged) AND obj-c35-savedev-zljoin's hot inlet (new) -- both writes are independent,
  no ordering dependency between them, so simple fan-out is safe here (unlike the
  write-then-rebuild case elsewhere, which genuinely needs a `t` to sequence).
- RECALL: obj-c35-rcl-slotplus1 (slot number) now also fans out to obj-c35-ccdevice's
  lookup (in addition to the existing obj-c35-ccvalues lookup) -> `prepend symbol` ->
  obj-c34-indev inlet 0, selecting the saved device by name.
Confirmed via diff: adding obj-c35-savedev-zljoin's tap on obj-c34-indev's outlet 1, and
the two new incoming wires into obj-c34-indev's inlet 0 (bang-for-save, select-by-name-
for-recall), are the ONLY changes touching this pre-existing object -- no existing wire on
obj-c34-indev was removed or altered.

### Program Change Input Device selector repositioned in presentation
Same message: asked for obj-c35-pcindev (added earlier this session) to be moved next to
the bus-state preset controls (obj-pv2-slotmenu/SAVE/RECALL/nameedit, all at presentation
x=750) rather than sitting in the MIDI Control Input area -- it drives Program Change
recall, not CC control, so it belongs with the preset UI it actually affects. Moved to
presentation_rect [750, 165] (comment) / [750, 183] (umenu), just below the existing
nameedit row at [750, 140], clear of every other control in that column. Adjusted the
comment text to match its new context (removed a reference to "the MIDI Control Input
Device above", which no longer applies once it's no longer physically below that control).
RETEST: this is a pure presentation-layout change plus two new coll-based read/write
paths -- needs a full real-Max retest: SAVE/RECALL a CC-mapping preset with the MIDI
Control Input Device set to something, switch it to a different device, RECALL, confirm
it switches back; and confirm the Program Change Input Device umenu now appears in the
right spot in presentation mode.

### Bug found on first test of the device-name feature — fixed same session
User's report: "Presets dont work with the midi input umenu" (and, pointedly, "I think you
are quite inefficient with basic max" — fair, given what this bug turned out to be).

**Root cause: umenu does not respond to a bare `bang`.** The SAVE mechanism banged
`obj-c34-indev` directly to make it "re-output its current selection", based on a WebSearch
result quoted earlier this session ("whether activating or deactivating, the object also
sends its current message..."). That quote was almost certainly describing umenu's popup
open/close (activate/deactivate) behavior, NOT a runtime bang message — re-checked
directly against Cycling '74's own documented umenu message list (append, clear, delete,
dictionary, prefix, set, symbol, etc.) and bang is not among them. Banging obj-c34-indev
did nothing at all, so obj-c35-savedev-zljoin's cold inlet (the device name) was never
actually fed anything, and every SAVE wrote garbage/empty device data into
obj-c35-ccdevice — exactly matching "doesn't work" (RECALL's `symbol <name>` would never
match a real device).
Fixed by using the `value <name>` object instead — Max's global-variable object, which
Cycling '74's reference explicitly confirms DOES respond to bang ("you can get the
contents out of a particular value object by sending it a bang"), and which this exact
patch already uses successfully elsewhere (`value dfx_bus12_1_label_cc` etc., set from
many message-box sources and read via bang). Added obj-c35-indevname-value (`value
c35_indevname_shadow` — confirmed globally unique against this patch's other 5 `value`
object names before using it), continuously updated from obj-c34-indev's outlet 1 (the
device name text) on every real selection change, and banged on SAVE (save-t's outlet 9)
to fetch whatever it last saw. RECALL's `prepend symbol` -> obj-c34-indev path is
unchanged — that part relies on the "symbol" message, which IS consistently documented
across multiple independent searches, unlike the bang assumption.
Lesson applied going forward: when a Max object's behavior for a specific message is
being assumed rather than freshly confirmed against its actual documented message list,
check the message list directly (as done here, and as should have been done the first
time) rather than trusting a single search snippet that could be describing something
else entirely.
RETEST: needs the exact same real-Max retest described above (SAVE with device A
selected, switch to device B, RECALL, confirm the umenu switches back to device A) —
this is now the second attempt at this specific mechanism.

### Second attempt also wrong — over-engineered where a direct wire would do
User's report: "It's not working" (still), plus a pointed, correct simplification: "For
the umenu it is as simple to send a symbol message with the symbol you want to select
and send from the umenu" — i.e. RECALL's `symbol <name>` is fine as-is, but SAVE doesn't
need any bang-driven fetch mechanism at all.
Realized on reflection: `obj-c34-indev`'s outlet 1 already broadcasts the selected item's
text continuously, on every real selection change — exactly like any cold inlet, it just
needs to be wired directly into `obj-c35-savedev-zljoin`'s cold inlet ONCE, and it will
always hold the current device name by the time SAVE's hot (slot) trigger fires, with no
fetch step needed. The `value c35_indevname_shadow` proxy object and its bang-on-outlet-9
step (added to work around the wrongly-assumed "bang the umenu" mechanism) were entirely
unnecessary complexity once the actual bug was fixed — removed both. obj-c35-save-t
reverted from 10 outlets back to 9. Net result: one fewer object than the previous
attempt, and a genuinely simpler, more obviously-correct design — this is what should
have been built the first time.
Also used this round to re-audit the whole Control-35 build for orphaned/dead objects
left over from earlier iterations this session (there have been several rounds of
rewrites to this specific feature) — none found; diff against Control-34 confirmed still
purely additive (40 boxes added, 0 removed, only the 2 expected filename-text changes to
existing objects) with obj-c34-indev now carrying exactly 3 wires total: the pre-existing
one to obj-c34-in-pport, the new continuous tap to savedev-zljoin, and the new
select-by-name wire from RECALL.
RETEST: third attempt at this specific mechanism — same test as above (SAVE with device A
selected, switch to device B, RECALL, confirm it switches back to device A).

⚠ Separately and more urgently: the user reports the edit-mode/presentation-mode
performance problem (previously "sluggish... 40GB+ memory") has gotten WORSE — "the
moment I go into edit mode I barely can go back to presentation" — to the point that it
is now blocking them from testing ANY of this session's changes in real Max at all. This
was investigated structurally in the original "35" performance section above (flat
790-object patch, 46 live.* objects, 52 parameter-enabled objects) with no confirmed root
cause and a recommendation NOT to blindly restructure the existing baseline. Given it has
now escalated to a testing-blocking severity, and this session still cannot run Max to
verify anything directly, treat this as the top-priority open question for the next round
of contact with the user — see the reply for what was asked back.

## 34: MIDI Control Input + 128 presets with renaming
Generated by `sp404gen/build_control34.py` (from Control-33); structurally validated by
`sp404gen/validate_control34.py`. Full diff against Control-33 confirmed: 62 boxes added
(obj-c34-* namespace), 0 removed, 81 lines added, 0 removed, and exactly 4 field changes
on existing boxes (presets filename x2, `obj-pv2-pgsplit` range, `obj-pv2-slotmenu` items).

**(a) MIDI Control Input.** Lets an external MIDI controller drive the 6 dials, the EFX
select umenu, and the EFX on/off toggle — deliberately NOTHING else (not bus select, not
DFX slots, not presets), per the user's explicit scope. New "MIDI Control Input Device"
umenu (`obj-c34-indev`, item list cloned from the existing "MIDI Output Device" umenu
`obj-4` — same caveat as that one: it's a snapshot of the user's own MIDI ports captured
at some point, likely needs re-picking on whatever machine actually runs this) → `prepend
port` → one bare `ctlin` (all channels/controllers on the selected port; channel is
deliberately NOT filtered — the user didn't ask for it and SP-404 channel dispatch is an
output-side concept, unrelated to accepting external control input).
For each of the 8 targets: a `number` box ("MIDI CC selector") defaulting to the CC the
patch already sends to hardware for that control (16/17/18/80/81/82 for ctrl1-6, 83 for
EFX select, 19 for EFX on/off — confirmed against the actual `ctlout` objects and the
existing "Ctrl 1-3 = CC#16-18..." comment, not assumed). Per target: `ctlin`'s
controller-number outlet → `==` (hot inlet 0) against the CC selector (cold inlet 1) →
drives a `gate 1`'s control inlet 0; `ctlin`'s value outlet → the gate's data inlet 1.
Ordering relies on `ctlin` firing right-to-left (channel, then controller#, then value —
same guarantee already used everywhere else in this patch): controller# always reaches
`==`/gate-control before value reaches gate-data, so the gate's open/closed state is
always current before data arrives. When CC# matches, the raw value passes straight into
the target's own hot inlet 0 — the SAME inlet the user's mouse edits use — so external
control audibly sends CC out and updates pattrforward/pattrstorage exactly like a live
edit (this is deliberate: it's how "only these 8, and only via their real outputs" is
satisfied with no separate code path). EFX on/off gets an extra `>= 64` threshold
(`obj-c34-onoff-thresh`) converting ctlin's 0-127 value to live.toggle's 0/1.
⚠ UNVERIFIED: the assumption that live.dial/live.menu's own numeric range (already 0-127,
confirmed since their own outlets feed `ctlout` directly with no scale object) means a raw
incoming CC value can be fed straight into their hot inlet with no conversion. This is a
direct extension of already-verified patch behavior (same idiom used for DFX preset
restore in Control-33) but has never been tried with genuinely-external, unpredictable
input before.

**(b) 128 presets + renaming.** `obj-pv2-pgsplit` widened `split 0 7` → `split 0 127`
(PC 0-127 now all recall a preset; was PC 0-7 only). `obj-pv2-slotmenu` grows from 8
static items to 128 ("Preset 1".."Preset 128" defaults). Preset NAMES are intentionally
NOT part of the pattrstorage-based `Control34_presets.json` (that file is parameter data,
saved/loaded independently) — they live in a separate embedded `coll`
(`obj-c34-namecoll`, `@embed 1`, same embedding mechanism already proven via the existing
`lbl_k0`-style colls), keyed 1-128, storing ONLY the free-text part of each name
(default "Preset N"). The PC-number prefix is NEVER stored — it's recomputed as
(slot - 1) every time the menu is rebuilt from the coll, so a rename can never desync the
prefix from the actual slot/PC number.
Rebuild routine (`obj-c34-rebuild-t`/`-uzi`/`-split`/`-minus1`/`-sprintf`/
`-prependset`): "clear" the umenu, then `uzi 128 3` walks keys 1-128, building each
item as "append PC<n> - <name...>" and appending it. Uses a single-specifier
`sprintf PC%ld -` (same proven pattern as the existing `sprintf send Bus%d_X` objects —
deliberately AVOIDING an untested `sprintf ... %ld ... %s` combination) whose output
("PC<n> -") is fed into a bare `prepend`'s COLD (right) inlet, dynamically re-arming what
it prepends; the coll's looked-up name then hits `prepend`'s HOT (left) inlet, producing
"append PC<n> - <name...>" as one message straight into the umenu. Runs once on load
(`obj-c34-lb2`) and once after every rename.
Rename UI: `obj-c34-nameedit` (a `textedit` — genuinely novel, zero precedent elsewhere
in this codebase) outputs free text (no PC number typed by the user) → `obj-c34-namet`
(`t b b l`, right-to-left): text is stored into `obj-c34-namepack`'s cold inlet FIRST,
then the current slot (from `obj-c34-slotshadow`, the same cold-tapped "shadow int" idiom
used throughout this patch, +1) triggers `namepack`'s hot inlet to write [slot, name]
into `namecoll`, and only THEN (last) is the full rebuild kicked off — guaranteeing the
rebuild's coll lookups always see the just-written name. After the rebuild's "clear" wipes
the visible selection, `obj-c34-slotshadow` is banged again (from `uzi`'s done-outlet 1)
and `prepend set`s it back onto the umenu, restoring the highlighted item's (freshly
rebuilt) text.
⚠ UNVERIFIED, in order of risk: (1) `textedit`'s actual output behavior/timing (does it
fire on Enter? Tab? focus-loss only? all three?) — completely untested in this codebase;
(2) `prepend`'s right-inlet dynamically changing its stored prefix at runtime — standard
documented Max behavior but never exercised here before; (3) `uzi 128 3`'s outlet 1 being
the "done" bang (inferred from Max's standard 2-outlet uzi semantics, extrapolated to the
3-outlet form — this codebase's existing `uzi 5 3` usage never wired outlets 0/1 at all,
so there's no in-file precedent to confirm against).

Presets companion file renamed to `Control34_presets.json` (read/write msgs) — format
unchanged, still has no name/DFX data (names live in the embedded coll, not this file).

**⚠ MAX-TEST ITEMS (Control-34):**
a) Console clean on load (in particular: does `coll obj-c34-namecoll` load its 128
   embedded default entries correctly, and does the initial "clear + 128x append" rebuild
   run without error and show "PC0 - Preset 1".."PC127 - Preset 128"?).
b) MIDI Control Input: pick a real device in "MIDI Control Input Device", send CC16 (or
   whatever ctrl1's selector shows) from it — confirm ONLY ctrl1's dial moves and CC16 is
   the only thing transmitted (watch for the double-send bug class from Control-32 —
   this is a NEW input path, re-verify it doesn't trigger it). Repeat for all 8 targets,
   including EFX select (send a CC83 value bigger than the current bus's item count —
   confirm it doesn't do anything worse than silently fail/clamp) and EFX on/off (confirm
   the 64-threshold feels right, adjust if the controller's buttons send something
   unexpected). Change a CC selector's number away from default, confirm the NEW number
   is what actually triggers control (and the old CC# no longer does).
c) 128 presets: SAVE to preset 127 (PC 126), RECALL it, confirm no off-by-one. Send raw
   PC 127 into the patch's PC-recall input, confirm it recalls preset 128, not an error.
d) Renaming: select a preset, type a name in the textedit, press Enter — confirm the menu
   immediately shows "PC<n> - <name>" with the SAME slot still selected. Close and reopen
   Max — confirm the renamed label survived (embedded coll, saved with the .maxpat
   itself, independent of Control34_presets.json). Try renaming while a DIFFERENT preset
   is selected than the one just recalled/saved, to make sure the slot being renamed is
   always the one shown in the menu, not some other cached slot.

### Rebuild bug found in first Control-34 Max test, fixed same session
User-reported symptoms: MIDI Control Input worked correctly first try (item (b) above,
confirmed). The 128-preset system was completely broken: the slotmenu umenu was
unresponsive with nothing selectable, and SAVE/RECALL did nothing (so renaming, item (d),
couldn't even be attempted).

Root cause (one bug, both symptoms): `obj-c34-rebuild-sprintf` was built as
`sprintf PC%ld -` — missing the literal `append` selector word. Every one of the 128
rebuild messages sent to slotmenu therefore looked like `"PC5 - Preset 5"` instead of
`"append PC5 - Preset 5"`. A umenu doesn't recognize `PC5` as a message selector (it's
neither a known command like `append`/`clear`/`set` nor a bare int), so Max silently (or
with a console error) dropped every single append — after the load-time `clear`, the
menu was left with exactly 0 items. That explains BOTH symptoms directly: an empty umenu
has nothing to show when clicked ("unresponsive, nothing selectable"), and SAVE/RECALL
(which work by banging slotmenu to re-assert its current selection before building the
`store N`/`recall N` message) get no output from an item-less umenu, so the store/recall
messages never receive a valid slot argument.
Fix: `sprintf append PC%ld -` (the `append` word lives in the format string itself, since
the downstream `prepend` object only ever prepends whatever text it was last given — it
was never going to add "append" on its own).

While fixing this, also caught a second, latent bug in the same area (not yet visible to
the user, since it requires either a first-ever load or a rename to trigger, but would
have corrupted preset names silently): `obj-c34-slotshadow`'s single outlet fed BOTH the
rename-store chain (`slot1-name` → `namepack` → `namecoll`) AND the post-rebuild
selection-restore chain (`restore-pset`). Since the rebuild's "restore selection" step
bangs this shadow on EVERY rebuild (at load, and after every rename), it was also firing
the rename-store chain every time — writing whatever stale (or, on first load,
uninitialized) value happened to be sitting in `namepack`'s cold inlet into `namecoll` at
the current slot, silently overwriting that preset's name. Fix: split into two separate
shadow ints, `obj-c34-slotshadow` (rename-fetch only) and `obj-c34-slotshadow-restore`
(selection-restore only), both cold-tapped from the same slotmenu output but with no
shared consumer — mirrors the "hw-holder vs display-shadow" split already used elsewhere
in this patch specifically to prevent this class of cross-talk.

Both fixes are in `build_control34.py`; Control-34 regenerated (779 objects, 1241 lines)
and re-validated (`validate_control34.py` now also asserts the sprintf text contains
"append" and that the two shadows have no cross-wiring).
RETEST: umenu must show all 128 items ("PC0 - Preset 1".."PC127 - Preset 128") on load
and be selectable; SAVE/RECALL must work; then retest renaming (MAX-TEST item d above),
which couldn't be attempted at all before this fix.

### Second rebuild bug found on retest, fixed same session — prepend abandoned
User retested after the fix above: still broken, but now specifically "just the preset
menu" (confirmed everything else -- dials, bus select, MIDI Control Input -- unaffected),
with the Max console flooded with `umenu: doesn't understand "Preset"` on load.

That error is the smoking gun: the message actually reaching slotmenu started with the
bare word `Preset`, not `PC5` (the previous bug) and not even `append PC5 -`. This means
`obj-c34-rebuild-prependset` (a bare `prepend`, no fixed argument, whose right/cold inlet
was fed `"append PC<n> -"` at runtime to dynamically set its prefix) never actually
adopted that prefix — it silently stayed empty and just passed the coll's raw stored
value (`Preset5`, one atom) straight through untouched. The "dynamically re-armed
prepend" technique documented as unverified in the original "34" section write-up (⚠
UNVERIFIED item 2) was in fact the bug: `prepend`'s right inlet does not take an
arbitrary multi-atom message and adopt it as a new multi-word prefix the way a
fixed creation argument does.

Fix: abandoned `prepend` for this entirely. Replaced the two-object
sprintf-then-prepend chain with a SINGLE dual-specifier sprintf,
`sprintf append PC%ld - %s` — the `%ld` (PC number) and `%s` (coll-stored name) are
combined in one object, with no intermediate hand-off between objects to get wrong.
%ld is the first specifier (hot/leftmost inlet 0, fires last and triggers the output);
%s is the second (cold/rightmost inlet 1, fires first). `obj-c34-rebuild-split` (`t i i`)
still sequences this: outlet 1 (FIRST) → namecoll lookup → sprintf's cold inlet 1;
outlet 0 (SECOND) → `- 1` → sprintf's hot inlet 0, producing
`"append PC<n> - <name>"` straight into slotmenu, no `prepend` involved at all.
Also fixed while in there: `obj-c34-namecoll`'s default values are now exactly ONE atom
per entry (`Preset5`, not `['Preset', '5']`) — `%s` substitutes exactly one atom, so the
old 2-atom default would have silently dropped the number from every un-renamed preset's
displayed name (latent, would not have been caught by the console-error symptom above).
`obj-c34-rebuild-prependset` no longer exists. `validate_control34.py` updated to check
the exact sprintf text, that prependset is gone, and that every namecoll value is a
single atom.
RETEST: same as above (umenu must show all 128 items and be selectable; SAVE/RECALL must
work; renaming must work and show "PC<n> - <name>" immediately) — this is the third
attempt at the same test, unverified until confirmed in Max.

### Third and fourth bugs found on retest, fixed same session — root-caused via docs, not guessing
User retested: (1) all 128 items now show and are selectable, but numbering was shifted —
started at "PC2 - Preset 3", ended at "PC129 - Preset 128"; (2) SAVE/RECALL confirmed
working; (3) renaming still did nothing at all.

This time, instead of guessing again, looked up Max's own `uzi` and `textedit` reference
documentation (docs.cycling74.com, via web search) before touching code. Both remaining
bugs turned out to be misunderstandings of documented object behavior, not timing/ordering
issues like the first two:

**Bug 3 — `uzi`'s second argument is a counter BASE VALUE, not an outlet selector.**
`obj-c34-rebuild-uzi` was built as `uzi 128 3`, intended as "128 repetitions, counter on
the 3rd outlet". That's not what the argument means: uzi's outlets are always exactly
[bang-repeat, done-bang, counter] regardless of arguments (docs: "the uzi object has
three outlets"); the actual syntax is `uzi <repetitions> <base>`, where the OPTIONAL
second argument sets the STARTING VALUE the counter counts from (default 1). `uzi 128 3`
therefore counted 3,4,...,130 (128 values) instead of 1,2,...,128 — exactly matching the
reported shift: first entry from counter=3 (PC=2, namecoll(3)="Preset3"), last entry from
counter=130, whose PC number computes to 129 but whose namecoll lookup (key 130, past the
128 valid keys) fails and silently leaves the cold %s inlet holding the last successful
lookup's value ("Preset128" from counter=128) — reproducing "PC129 - Preset128" exactly.
Fix: `uzi 128` (no second argument, defaults the base to 1).
Note: this is a mistake specific to writing NEW code with the wrong mental model of the
argument — not something inherited from Control-32/33's existing `obj-pv2-hw-uzi`, which
was re-checked and is simply `uzi 5` (correct, no base argument) despite an earlier,
mistaken debugging note in this session claiming otherwise (misread the debug print's own
`numoutlets` field as part of the object's text).

**Bug 4 — `textedit` defaults to `keymode 0`, where Return never outputs anything.**
`obj-c34-nameedit` was created with no attributes at all. Per Max's textedit reference,
`keymode 0` (the default) treats Return as a normal line break with NO output; only
`keymode 1` makes Return "output the entire contents of the current buffer". Without it,
the entire rename chain (`namet` → `namepack` → `namecoll` → rebuild) could never fire no
matter what the user typed — this fully explains "renaming doesn't work" as a totally
separate bug from bug 3 above (unrelated to the uzi/numbering issue).
Fix: added `keymode: 1` to `obj-c34-nameedit`'s box definition.

Both fixes are minimal, single-field changes (`build_control34.py` diff: `obj-c34-rebuild-
uzi` text `'uzi 128 3'` → `'uzi 128'`; `obj-c34-nameedit` gains `keymode: 1`; nothing else
changed). `validate_control34.py` now asserts both.
RETEST: (i) menu must read "PC0 - Preset 1" through "PC127 - Preset 128", in order, no
stale/duplicated trailing names; (ii) SAVE/RECALL (already confirmed, shouldn't have
changed); (iii) select a preset, type a name, press Enter/Return — the menu should
immediately show "PC<n> - <name>" with the same slot still selected. This is the fourth
attempt at this specific test.

### Fifth bug found on retest — the keymode/outputmode attributes never actually applied
User retested: (i) numbering fully correct now, "PC0 - Preset 1" through
"PC127 - Preset 128", no stale names — bug 3 confirmed fixed. (ii) SAVE/RECALL still
fine. (iii) Renaming still broken, but differently: pressing Return just inserts a line
break in the text box (not a commit) exactly like keymode 0's documented behavior; only
clicking OUTSIDE the text box (defocus) commits anything. And what gets committed is
wrong: the PC-number prefix is fine, but the name portion always shows the literal word
"text" instead of whatever was typed.

Both symptoms point to the same root cause: `keymode: 1` and (this session, in response)
`outputmode: 1`, set as top-level JSON keys on obj-c34-nameedit's box definition (the same
mechanism used successfully for every other UI attribute in this codebase, e.g. umenu's
`items`, live.dial's `parameter_enable`), did NOT take effect for `textedit` specifically.
The object kept its defaults: keymode 0 (Return does nothing but insert a line break --
matches "changes paragraph" exactly) and outputmode 0 (output is a `"text <words...>"`
MESSAGE, not a bare symbol -- confirmed against a Max forums thread specifically about
this exact "text" prefix gotcha). Since `obj-c34-namepack` (`pack 0 s`) only keeps the
FIRST atom sent to its symbol inlet, receiving `["text", "MyName"]` silently kept just
"text" and discarded the actual typed name -- reproducing "always text regardless of
what you typed" exactly. (PC-number prefix was unaffected since it's computed independently
by the rebuild's sprintf, not part of this chain.)

Fix: abandoned the creation-time attribute keys entirely. Max's own textedit reference
documents BOTH settings as also configurable via an explicit MESSAGE to the object's
inlet ("the message keymode 1 causes..."), which is a different, message-passing
mechanism from a saved creation attribute -- added `obj-c34-nameedit-lb` (a dedicated
loadbang) feeding two message boxes, `keymode 1` and `outputmode 1`, both wired into
`obj-c34-nameedit`'s inlet at load. `validate_control34.py` updated to check these
messages and their wiring instead of the (apparently inert) `keymode` JSON key.
RETEST: press Return in the rename box -- must commit (not insert a line break); the
resulting preset label must show the actual typed name, not the word "text". This is the
fifth attempt at the renaming half of this test; numbering and SAVE/RECALL are confirmed
already and don't need retesting.

### Sixth bug found on retest — outputmode never fixes it; textedit's "text" prefix is unconditional
User retested: Return now commits (bug 4's keymode fix confirmed working). But the
"text" problem persisted exactly as before -- typed name still replaced by the literal
word "text" -- meaning the `outputmode 1` message had no effect either.

Looked this up properly this time (a Max forums thread titled almost exactly "How to
delete 'text' that is prepended to output of textedit object?"): `textedit` ALWAYS
prepends the literal selector `text` to whatever it outputs, as an actual message
selector -- e.g. typing "MyKit" produces the message `text MyKit`, not the symbol
`MyKit`. This is NOT something `outputmode` turns off (that assumption, made when fixing
bug 4, was simply wrong -- `outputmode` governs a different aspect of formatting, not
whether the "text" selector is present). The confirmed, standard fix used throughout Max
patches for this exact behavior is a `route text` object, which matches the "text"
selector and forwards everything after it, stripped, through its first outlet.

This also surfaced a second, previously-latent defect: preset names can be MORE than one
word (multiple atoms, once "text" is stripped by route), but `obj-c34-namepack` (`pack 0
s`) only keeps the first atom sent to a symbol inlet, and the rebuild's `sprintf`'s `%s`
specifier also only substitutes exactly one atom -- both would have silently truncated
any multi-word rename to its first word even after the "text" bug was fixed. Fixed both
at once:
- `obj-c34-route-text` (`route text`) inserted between `obj-c34-nameedit` and
  `obj-c34-namet`, stripping the "text" selector before anything else sees the message.
- `obj-c34-namet` restructured `t b b l` → `t b l b`: out2 (FIRST) arms `obj-c34-namepack`
  cold inlet with the current slot number (a SINGLE atom -- this direction of dynamic
  `prepend` re-arming is fine, unlike bug 2's failed multi-atom attempt); out1 (SECOND)
  passes the actual typed words (list, any length) into `namepack`'s hot inlet, producing
  `[slot, word1, word2, ...]` into `namecoll`; out0 (LAST) kicks off the rebuild.
  `obj-c34-namepack` changed from `pack 0 s` to a bare `prepend`.
- Rebuild routine: `obj-c34-rebuild-sprintf` reverted to single-specifier
  `sprintf append PC%ld -` (just the PC number again); combining it with the coll's
  (possibly multi-atom) stored name is now `obj-c34-rebuild-zljoin` (`zl.join`), which
  concatenates two lists of any length -- left/hot inlet is the first segment and
  triggers output, right/cold inlet is the second segment. No `%s`, no re-armed
  `prepend`, no atom-count assumptions anywhere in this path anymore.
- Removed `obj-c34-nameedit-outmode` (the ineffective `outputmode 1` message) entirely.
`validate_control34.py` updated throughout for the new object names and wiring.
RETEST: rename a preset to something with a SPACE in it (e.g. "My Kit") -- the full
phrase must appear, not just the first word, not the word "text". This is the sixth
attempt at the renaming half of this test.

### Rebuilt from scratch on pattrstorage's own native slot-naming protocol
User retested: renamed labels showed as e.g. "PC0 - symbol Preset1" (the literal word
"symbol" appearing, not the typed text), and pressing Return in the textedit box did
nothing at all -- both regressions from the previous (confirmed-working) round, and both
inside the from-scratch coll/uzi/sprintf/zl.join system this session had been iterating
on for five rounds already.

The user also supplied a working reference patch of their own (`MIDI_CC_scene_morph.maxpat`,
uploaded, not committed to this repo) that renames a pattrstorage-backed menu. Tracing it
revealed something this session didn't know: **`pattrstorage` has a native slot-naming
protocol**, independent of the classic grid `preset` UI object --
- `getslotnamelist` sent to pattrstorage makes it emit, via its own outlet, one
  `slotname <preset#> <name>` message per slot, followed by a bare `slotname done`.
- `slotname <preset#> <name>` sent TO pattrstorage renames that slot.
This is a real, native pattrstorage feature (confirmed by the reference patch sending it
directly to a bare `pattrstorage <name> @savemode 2` object, the same kind of object this
patch already uses as `obj-pv2-pattrstorage` for its main preset values) -- not something
that needs a custom coll, a rebuild loop, or careful multi-object hand-offs to reinvent.
The entire from-scratch rename/rebuild system (namecoll, uzi, sprintf, zl.join, prepend,
the shadow-int cross-talk fixes) is now DELETED and replaced with wiring adapted directly
from the reference patch's `obj-22`/`obj-13`/`obj-30` (rename-send) and `menu-helper`
(clear/gate/rebuild) subpatchers:

- Rename-send: `obj-c34-slot1` (`+ 1`) continuously tracks the current 1-based slot
  (no bang-fetch needed -- fed directly and hot from `obj-pv2-slotmenu`'s own outlet, so
  by the time the user finishes typing it's already current) -> `obj-c34-namepack`
  (`pack s i`, hot=text/cold=slot#) -> `obj-c34-namemsg` (message `slotname $2 $1`,
  reordering pack's `[text, slot]` into pattrstorage's own `slotname <slot> <text>`
  syntax) -> `obj-c34-name-t` (`t b l`, right-to-left): out1 (FIRST) sends the rename
  straight into `obj-pv2-pattrstorage`; out0 (SECOND) triggers a refresh once the rename
  has actually landed.
- Refresh (shared by loadbang and post-rename): `obj-c34-refresh-t` (`t b b`): out1
  (FIRST) -> `obj-c34-clearopen` (`t 1 clear`) clears the menu and opens
  `obj-c34-gate`; out0 (SECOND) sends `getslotnamelist` to pattrstorage.
- Parsing pattrstorage's reply (a NEW tap on `obj-pv2-pattrstorage`'s existing outlet,
  purely additive -- nothing existing was touched): `obj-c34-slotname-route`
  (`route slotname`) -> `obj-c34-slotname-done` (`route done`): matched "done" closes
  the gate (`obj-c34-gateclose`, `t 0`) AND restores the visible selection
  (`obj-c34-slotshadow`, banged, -> `prepend set` -> slotmenu); unmatched `<n> <name>`
  goes to `obj-c34-slotname-unpack` (`unpack 0 s`), whose two ALREADY-SEPARATE outputs
  (int n, symbol name -- no coll, no %s, no re-armed prepend needed) feed
  `obj-c34-slotname-minus1`/`obj-c34-slotname-sprintf` (`sprintf append PC%ld -`) and
  `obj-c34-slotname-zljoin` (`zl.join`, concatenating the PC-prefix with the name)
  respectively, landing on `obj-c34-gate`'s data inlet -> `obj-pv2-slotmenu`.
- `obj-c34-nameedit` gains `outputmode: 1` as a creation-time attribute (matching the
  reference patch's own working config) IN ADDITION to `route text` -- the reference
  patch keeps `route text` too despite `outputmode 1`, confirming (again) that
  `outputmode` does NOT remove textedit's unconditional `text` selector; only `route
  text` does.

This removes ALL of: `obj-c34-namecoll`, `obj-c34-slotshadow-restore`, `obj-c34-namet`,
`obj-c34-slot1-name`, and the entire `obj-c34-rebuild-*`/`obj-c34-lb2`(old)/`obj-c34-clear-msg`
loop-based rebuild chain. `validate_control34.py` rewritten to match.

⚠ UNVERIFIED, genuinely new risk: whether a BARE `pattrstorage` object (not bound to the
classic grid `preset` UI object, which is what the reference patch actually uses)
enumerates a full 1-128 slot range via `getslotnamelist` from a fresh/never-saved-to
state, or only reports slots that have actually been stored to at least once. If only
touched slots appear, the menu will show fewer than 128 items until each has been saved
to once -- flagged as a MAX-TEST item below rather than guessed at further.
RETEST: everything above, from scratch -- (i) console clean on load; (ii) menu shows
placeholder/default names for all (or however many) slots pattrstorage reports, each
prefixed "PC<n> -"; (iii) SAVE/RECALL still work (unchanged code path); (iv) rename a
preset (including one with a space in the name) -- Return commits it, the menu shows the
real typed name immediately with the same slot still selected, and re-opening Max after
a SAVE should show the renamed label persisted (pattrstorage's own slot names are saved
as part of its own `@savemode`d file, same file this patch already reads/writes).

### Two more bugs found before the user could even retest: box-definition mismatch + missing SAVE-refresh trigger
User reported (before mentioning their own workflow detail): "umenu shows nothing" AND
"SAVE does nothing" -- SAVE being previously-working, completely unrelated functionality
regressing was the loudest signal something was structurally wrong, not just a rename
detail.

**Bug: `obj-c34-nameedit` (textedit) was declared with the wrong outlet count.**
Direct field-by-field comparison against the reference patch's own `obj-224` (the
textedit box this whole design was modeled on) showed: real Max `textedit` objects
always have 4 outlets (`outlettype: ["", "int", "", ""]`) -- this build had wrongly
declared `numoutlets: 1`. A box-definition mismatch for a UI object's inherent structure
(as opposed to, say, a `prepend`'s inlet count, which this codebase's own already-working
`obj-7`/`obj-pv2-pre-store`/etc. show Max tolerates being under-declared) is exactly the
kind of error that can break a patch's load in ways well beyond the one broken object --
plausibly explaining why previously-working SAVE also appeared dead. Fixed: numoutlets=4,
outlettype matching the reference exactly (only outlet 0 is wired, same as the reference).

**Missing: SAVE never refreshed the menu.** Separately, the user then explained their own
workflow: "I am saving new preset slots from the classic grid preset UI object and the
umenu gets auto populated." Their reference patch's classic grid `preset` object (which
this patch doesn't use -- Control-34 uses a plain umenu + explicit `store N` messages)
evidently refreshes its own bound menu automatically as part of its native store
behavior. This patch has no equivalent, so SAVE alone would never have populated the
menu even with the textedit bug fixed. Added: `obj-pv2-save-t` extended `t b b` ->
`t b b b` (identical technique to Control-33's own `obj-pv2-rcl-post-t` extension) -- the
new leftmost outlet fires LAST (after "store" has already reached pattrstorage) and
triggers the same refresh routine used after rename and at load.

RETEST: same items as above, now that SAVE should populate the menu for the first time --
in particular, does SAVE alone (without ever renaming) make a new slot appear in the menu
at all? That directly tests whether a bare `pattrstorage` enumerates a slot via
`getslotnamelist` once it's been stored to (the open question flagged above).

### Real progress: SAVE populates the menu; renaming has a slot-numbering off-by-one
User retested: menu now shows entries (`PC-1 - (undefined)`, `PC0 - <(unnamed)>`,
`PC1 - <(unnamed)>`, `PC2 - <(unnamed)>` after one SAVE) and SAVE/RECALL work -- the
bare `pattrstorage` DOES enumerate a range of slots via `getslotnamelist`, not just the
one just-stored slot, and it always reports a permanent slot 0 ahead of any user slots
(matches the reference patch's own item list, which likewise starts with `(undefined)`).
Renaming: the FIRST rename landed correctly (`PC-1` → `(first rename)`), but the SECOND
rename landed on the slot AFTER the one actually selected (`PC1` got renamed instead of
the selected `PC0`). The user noted their own reference patch has a similar quirk they
"kind of made work somehow."

Root cause: `obj-c34-slot1` (`+ 1`) assumed pattrstorage's `slotname` numbering was
1-based, copying the offset from the UNRELATED SAVE/RECALL `store N` convention (where
umenu index 0 → `store 1`, confirmed correct and separately validated over several
rounds). But `slotname`'s own numbering is 0-based, matching `obj-pv2-slotmenu`'s item
index directly -- confirmed by the observed data: slot 0 → `PC-1`, slot 1 → `PC0`,
slot 2 → `PC1` (i.e. PC = slot − 1, which the display side already computes correctly).
The FIRST rename only looked right by accident: `obj-c34-slot1` had never fired yet (the
user hadn't clicked anything), so it held its default 0, and slot 0 happens to be the
one you land on if you rename without ever selecting -- coincidentally correct, not
actually tracking anything. The SECOND rename used a real click on the displayed "PC0"
item (umenu index 1), and the erroneous `+1` computed slot 2 (`PC1`) instead of slot 1
(`PC0`) -- reproducing exactly "renamed the following preset."
Fix: `obj-c34-slot1` changed from `+ 1` to `+ 0` (a deliberate no-op, kept as an object
rather than wired straight through so the intent stays visible in the patch) -- the
SAVE/RECALL `+1` objects are untouched, since they target a different pattrstorage
message family (`store`/`recall`) that was already independently confirmed correct.

Also filtered pattrstorage's permanent slot 0 (`(undefined)`) out of the visible menu
entirely, since it isn't one of the intended 128 presets and would otherwise always sit
at the top of the list: `obj-c34-slotname-notzero` (`select 0`) inserted between
`unpack`'s int outlet and the PC-prefix/append path -- its reject outlet (non-zero slots)
passes through unchanged, its match outlet (slot 0) is left unwired, silently dropping
that entry before it ever reaches the menu.
RETEST: rename several DIFFERENT presets in a row (not just the first ever click) and
confirm each lands on the slot actually selected, not the next one; confirm the
permanent "(undefined)" slot 0 no longer appears in the menu at all.

### Retest: mostly good news, textedit doesn't clear itself between renames
User retested and reported three things, one screenshot resolved all of them at once:
"(undefined)" is gone (slot-0 filter confirmed working); the menu "stops populating" at
PC10; and "renaming does nothing." The screenshot showed PC0 through PC10 (11 items,
matching however many times SAVE has been pressed across testing so far -- NOT a
truncation bug, just an accurate reflection of what's been saved) and, critically, PC0's
label read **"Downer my"** -- a real custom name, proving renaming DOES actually commit.

"Downer my" is the tell: `textedit` never clears its own buffer after a rename lands.
"Downer" was leftover text from an earlier interaction (plausibly typed and abandoned, or
partially entered previously); "my" is what was actually typed in the reported "does
nothing" attempt; Return appended it to the stale leftover and renamed the slot to the
concatenation. So the previous "does nothing" report was actually "worked, but silently
combined with old text still sitting in the box" -- visually indistinguishable from truly
doing nothing if you're not looking closely at exactly what ends up in the label.

Fix: `obj-c34-name-t` extended `t b l` -> `t b l b` (existing outlet indices 0 and 1 left
untouched -- the new outlet was added at the front/rightmost position, which fires FIRST,
so nothing downstream needed remapping). The new first-firing action sends `"clear"` to
`obj-c34-nameedit`, wiping its buffer immediately after each rename, so the next thing
typed always starts from empty rather than silently prepending onto whatever was there
before.
RETEST: rename a preset, confirm the text box goes empty right after; type a second,
independent name into a DIFFERENT preset and confirm it lands as typed (not concatenated
with anything left over); the earlier retest items (off-by-one, "(undefined)" filter)
already confirmed working and don't need re-checking.

### Textbox-clear confirmed; slot-0 filter reintroduced the off-by-one from the OTHER direction; menu growth is capped by a real design conflict
User retested: (1) textbox clears itself -- confirmed fixed. (2) Renaming now targets the
slot BEFORE the one selected (the reverse of the original bug). (3) The menu is still
stuck at PC10 and doesn't grow further no matter how many more times SAVE is pressed.

**(2) is a direct, understood side effect of the slot-0 filter added two rounds ago.**
Filtering pattrstorage's slot 0 out of the REBUILT list (`obj-c34-slotname-notzero`)
means the umenu's own item POSITIONS no longer correspond 1:1 to real pattrstorage slot
numbers -- every visible item is shifted down by exactly the one slot that got omitted
from the front of the list (item 0 now shows what used to be item 1's data, etc). Since
`obj-c34-slot1` had been changed to `+ 0` (correct ONLY when item-position == slot-number,
which stopped being true the moment slot 0 was filtered out), renaming used the umenu's
raw position as the target slot -- one less than the real slot that position represents.
Fixed: `obj-c34-slot1` back to `+ 1` -- NOT for the original (wrong) reason ("pattrstorage
is 1-based"), but specifically to compensate for the slot-0 filter's effect on the
umenu's own indexing. Both `+0` and `+1` were each correct for a real reason at different
points in this design's evolution; this is now believed correct for the CURRENT
combination of both fixes together.

**(3) is a genuine, unresolved architecture conflict, not a simple bug** -- flagging for
the user rather than guessing at a fix, since resolving it means choosing between two
different designs:
The rebuild routine does `clear` then re-`append`s ONLY the slots pattrstorage currently
reports via `getslotnamelist` (confirmed by Max's own pattrstorage docs: this message
"causes all slots from 0 to the largest stored slot number to be output" -- i.e. it
reports a contiguous range up to whatever's been stored so far, nothing beyond). Since
`obj-pv2-slotmenu` is used as BOTH (a) the thing you click to pick a target slot for
SAVE, and (b) the thing that gets wiped and rebuilt from that same limited report, once
the first rebuild fires, the menu can never contain an item for a slot pattrstorage
doesn't already know about -- there is no way to select "the next new slot" through the
umenu, because that item doesn't exist to click on. The user's reference patch avoids
this entirely: its PRIMARY slot-selection UI is the classic grid `preset` object, which
always exposes a fixed set of slots (e.g. 128) regardless of what pattrstorage has
stored; its umenu is a secondary, supplementary display, not the only way to reach a new
slot. This patch has no equivalent of that grid object.
Two ways to resolve this, needing a decision:
  a) Keep the menu dynamic (grows only as slots are actually used, matching the reference
     patch's own philosophy) but add a SEPARATE, always-available way to target a
     specific slot number for SAVE (e.g. a plain number box 0-127 next to SAVE, bypassing
     the umenu for slot selection while still using the umenu for display/RECALL/rename).
  b) Go back to always showing all 128 items (this patch's ORIGINAL design before the
     pattrstorage-native rewrite), merging pattrstorage's reported names onto a
     permanent 128-slot base list instead of replacing the list outright on each
     rebuild -- closer to the previous coll-based approach, but sourcing names from
     pattrstorage's own protocol rather than a separate custom store.
Neither has been implemented yet -- asked the user which they'd prefer before proceeding,
since (b) is a substantial rebuild of the rebuild routine yet again and (a) is a smaller,
more surgical addition but changes the SAVE workflow (a separate slot-number field
instead of picking from the dropdown).

### User chose (b): always show all 128 slots -- rebuilt on a two-phase capture/rebuild design
Asked the user directly rather than guess; they chose to keep the always-128 display
(closer to this patch's ORIGINAL, pre-pattrstorage-native design) over adding a separate
slot-number field for SAVE.

New design -- CAPTURE then REBUILD, keeping pattrstorage's native slotname protocol as
the source of truth for names but no longer letting it dictate which slots are visible:
- **CAPTURE**: pattrstorage's "slotname <n> <name>" replies (from a `getslotnamelist`
  request) are written into `obj-c34-namecache` (a plain `coll`, @embed 1, pre-seeded
  with 128 default entries `['Preset', 'N']`) as they arrive -- this phase never touches
  the visible menu at all. `obj-c34-slotname-unpack` (`unpack 0 s`) splits each reply into
  the int slot number and the (possibly multi-atom) name; `obj-c34-cache-zljoin`
  (`zl.join`) builds `[n, name...]` for the coll's write syntax, ordered by unpack's own
  right-to-left firing (name into the cold inlet first, n into the hot inlet second,
  triggering the write).
- **REBUILD**: triggered only once the capture stream ends (pattrstorage's bare
  `slotname done`, meaning the cache is now fully up to date) -- `obj-c34-rebuild-t`
  clears the menu, then `obj-c34-rebuild-uzi` (`uzi 128`, base defaults to 1 -- the same
  base-value gotcha fixed earlier this session) unconditionally walks slots 1-128,
  looking up each one's name in `namecache` (ALWAYS finds something, real or the seeded
  default) and building `"append PC<n-1> - <name>"` via the same proven
  `sprintf append PC%ld -` + `zl.join` combination already validated for this exact
  purpose earlier in the session. This guarantees full 1-128 coverage every single time,
  regardless of what pattrstorage has actually been told to store.
- The old gate/clearopen mechanism (needed when live pattrstorage messages fed the menu
  directly) is gone entirely -- capture only ever writes to a coll, never to the menu, so
  there's no risk of a partially-open window letting stray data through.
- `obj-c34-slot1` stays `+ 1`, now for the simplest of its three justifications this
  session: the menu is a fixed, complete, in-order 128-item list again, so umenu index i
  is always real pattrstorage slot i+1 -- exactly the same convention SAVE/RECALL already
  use independently.
- Named defaults are 2-atom (`['Preset', 'N']`) again, not the single-atom form used
  right before this rewrite -- that single-atom form was specifically why a coll lookup
  came back wrapped as `"symbol <value>"` in an earlier round; multi-atom entries flow
  through `zl.join` exactly like a real (possibly multi-word) pattrstorage name would, so
  that quirk should not resurface, though it hasn't been re-confirmed by Max testing yet.
`validate_control34.py` rewritten for the new object graph; the old
`obj-c34-slotname-notzero`/`-minus1`/`-sprintf`/`-zljoin` (capture-time PC computation)
and `obj-c34-refresh-t`/`-clearopen`/`-gate`/`-gateclose` are asserted gone.
RETEST: from scratch -- (i) load shows all 128 items, "PC0 - Preset 1" through
"PC127 - Preset 128" for anything never touched by pattrstorage; (ii) SAVE to any slot,
confirm that slot's entry updates (even if just showing its default name -- pattrstorage
now knows it exists); (iii) RECALL still works; (iv) rename several DIFFERENT slots in a
row (including a multi-word name) and confirm each lands on the slot actually selected,
with the textbox clearing and the label updating correctly every time; (v) no stuck
count -- SAVE to slot 128 specifically and confirm it still shows correctly (the far end
of the range, not exercised by earlier rounds which only reached PC10).

### Retest: works, but single-word renames show a literal "symbol" prefix
User retested the two-phase capture/rebuild design: it works overall, but a renamed
preset displays as e.g. "PC4 - symbol MyKit" -- the literal word "symbol" prepended to
the name. The user correctly diagnosed the shape of the fix needed ("use the Route
object with symbol") even before full root-cause confirmation.

This is the SAME general class of bug as `coll`'s "symbol <value>" wrapping found (and
partially fixed) two rounds ago: a coll wraps a SINGLE-ATOM lookup/report as
`symbol <value>` rather than the bare value. That earlier fix only addressed
`obj-c34-namecache`'s DEFAULT seed values (changed to 2-atom `['Preset', 'N']`) -- it
missed that a genuinely single-WORD rename (e.g. "MyKit", exactly one atom once
`route text` strips textedit's own selector) hits the identical wrapping when captured
from pattrstorage's reply and written into the cache, regardless of what the defaults
look like.

Fix, per the user's own suggested approach: `route symbol` inserted at BOTH points a
single-atom value passes through the cache -- `obj-c34-capture-routesym` (before writing
a captured name into `namecache`) and `obj-c34-rebuild-routesym` (after reading a cached
name back out during the rebuild). Each one's matched outlet (strips "symbol") and
reject outlet (passes anything else through unchanged, e.g. multi-atom names) are wired
to the SAME destination, since exactly one of the two ever fires per message -- the exact
point coll applies this wrapping (on write vs. on read) wasn't nailed down precisely, so
stripping at both ends covers either possibility cheaply.
RETEST: rename a preset to a SINGLE word (e.g. "Bass") and confirm it displays as
"PC<n> - Bass", not "PC<n> - symbol Bass"; also re-check a multi-word name still displays
correctly (unaffected by this fix, but worth confirming route symbol's reject path
doesn't disturb it).

**CONFIRMED WORKING by the user (2026-07-12).** This closes out the 128-preset +
per-preset-renaming feature for Control-34 -- no further action needed on it unless new
issues surface. Remaining open items for this patch are hardware-only (see "NOT YET
tested" at the top of this file) and the pre-existing "REMAINING OPEN ITEMS" list further
down (DJFX Delay SYNC, Tremolo/Pan + Wah RATE display, etid32/33/34 breakpoints,
Tremolo/Pan WAVE RND question) -- none of which were in scope for this session's work.

## 33: SYNC-expr ternary fix + DFX slots into presets
Generated by `sp404gen/build_control33.py` (from Control-32); structurally validated by
`sp404gen/validate_control33.py`. Full diff against Control-32 confirmed: 11 boxes added
(obj-pv33-* namespace), 0 removed, and exactly 8 field changes on 6 existing boxes
(4 expr texts, read/write filename msgs, rcl-post-t rebuilt t b b → t b b b).

**(a) expr ternary fix.** All FOUR sync-detect exprs used the never-implemented C-style
ternary (`expr ($i1 >= 1) ? N : -1`) — not just the two (sl/sf) flagged in REMAINING
OPEN ITEMS; tc (TimeCtrlDly, 33) and zz (Zan-Zou, 34) had it too. All four rewritten as
`expr (($i1 >= 1) * (N+1)) - 1` (i1>=1 → N, else -1; verified arithmetically). This
means Category-C SYNC-mode display switching has silently never worked since Control-30
— it should start working for the first time in Control-33. Test it (see items below).

**(b) DFX slot assignments now in presets.** Closes the known Control-31/32 gap.
Wiring traced first: each DFX umenu (obj-43/84/125/166/207) outlet feeds ONLY
`select 1..42` → per-effect msg → `value dfx_bus12_N_label_cc` (silent store, read via
bang by per-bus label rebuild). NO ctlout anywhere in the chain — DFX assignment is
pure UI/label state, so preset restore is UI-only by design; there is no MIDI to send.
Mechanism mirrors pv2 exactly (obj-pv33-*): 5 hidden bg live.menu clients (varname
Dfx1..Dfx5, appended to the subscribe list — 45 names now), visible menu → bg inlet
(continuous write), bg out → shadow int COLD inlet (silent tap, same discipline as the
pv2 double-CC fix), post-recall bang → shadow HOT inlet → bare int into visible umenu's
hot inlet 0 — deliberately the SAME path as the patch's own loadbang defaults
(loadbang → msg '38' → obj-43), so the menu selects AND fires, rebuilding the
dfx value stores. Recall ordering: rcl-post-t now `t b b b` — out2 hw dispatch FIRST
(unchanged), out1 DFX restore SECOND (so dfx_* values are fresh), out0 current-bus
display refresh LAST (its effect-menu label rebuild reads those values).

**Presets file renamed** to `Control33_presets.json` (read/write msgs). An existing
Control32_presets.json can be migrated by renaming the file — format unchanged, old
files just lack Dfx1-5 (recall then leaves the DFX menus untouched).

### Cross-group bus-switch label scramble (user-found in first C33 Max test) — FIXED
Symptom: switching BUS1/2 → BUS3/4 or INPUT (any cross-GROUP switch) showed wrong
knob labels until the effect was manually re-selected; 1↔2 and 3↔4 were fine.
LATENT SINCE CONTROL-32 (nothing in C33's other two changes touches bus switching) —
the Control-32 no-hardware test pass evidently only exercised same-group switches.
Root cause: Max fires multiple cords from one outlet right-to-left by destination x,
ties bottom-to-top. From obj-12's outlet: obj-19 (x=260, menu item rebuild) first,
then obj-pv2-busadd (x=40, y=3400 — bottom-most of the x=40 ties) fired BEFORE the
five gate-control selects obj-506/544/582/620/c3132 (x=40, y=362..882). The pv2
refresh therefore injected the new bus's stored menu position into the 5 EFX gates
while the OLD bus's gate was still open, so it was translated through the OLD group's
position→effect mapping (busid_BUS12/BUS34/INPUT differ per group — same-group
switches share a mapping, which is why 1↔2/3↔4 looked correct). Re-selecting the
effect fired obj-18 with gates by then correct, which is why that "fixed" it.
Fix: `obj-pv33-busdefer` (deferlow) inserted between obj-12 and obj-pv2-busadd — the
entire pv2 bus-switch refresh now runs after obj-12's whole synchronous cascade
(item rebuild, gate flips, channel), independent of box positions. Recall's display
refresh enters at obj-pv2-busN below the deferlow and already ran in a deferred
context with gates matching the current bus — unaffected.
RETEST: cross-group switches (1/2→3, 1/2→INPUT, 3/4→1, INPUT→4 …) must now show
correct labels immediately; also re-check same-group switches and plain dial edits
for regressions (the refresh now lands one queue tick later).
**Retested by user (2026-07-09): confirmed working.**

### Post-SAVE/RECALL MIDI-channel-5 residue (user-found, 2nd C33 test) — FIXED
Symptom: after SAVE or RECALL, obj-15 (shared channel number box → all 8 ctlouts'
cold channel inlets) stayed at 5 regardless of the selected bus. Latent since
Control-32. TWO defects found:
(a) The post-recall sequence (hw dispatch + display refresh) was triggered from
    pattrstorage's NOTIFICATION outlet (out0 → rcl-defer, copied from v16). That
    outlet fires on store/write/read too — so SAVE ran the full 5-bus hw dispatch
    (channel ends at 5, all buses' CCs resent), and the load-time `read` plausibly
    dispatched MIDI on patch open (no-MIDI-on-load violation). Invisible in the
    MIDI-less v16 prototype and the no-hardware C32 tests.
    Fix: sequence now triggered by the two actual recall COMMAND paths — rcl-msg
    (RECALL button) and pgrcl (incoming PC) each also feed rcl-defer; the
    pattrstorage→rcl-defer notification wire is REMOVED. deferlow still guarantees
    the synchronous recall completes first. SAVE/store/read now trigger nothing.
(b) Recall's hw dispatch iterates obj-15 through 1..5 and nothing restored it —
    even the post-recall display refresh sent the viewed bus's CCs on channel 5.
    Fix: obj-pv33-busN-t (`t i i`) between busN and its 8 select fans: out1 (FIRST)
    → obj-15 (channel = bus number; ctlout channel inlets are cold → no MIDI),
    out0 (SECOND) → sel fans. Every display refresh (post-recall AND deferred
    bus-switch) now restores the channel before any audible refresh. On plain bus
    switches this duplicates the native obj-19→msg→obj-15 write (same value).
RETEST: (i) SAVE on any bus — channel must NOT change, and NO MIDI dispatch at all;
(ii) RECALL on each bus — channel must equal the viewed bus afterwards and live
edits go out on it; (iii) patch load — confirm no MIDI burst on open (this was
plausibly happening before via the read notification); (iv) PC-recall same as (ii).

**⚠ MAX-TEST ITEMS (Control-33, in addition to any unfinished Control-32 hw items):**
a) Console clean on load (no expr syntax errors anymore — their disappearance is
   itself the ternary-fix confirmation).
b) Regression: DFX menus still rebuild bus effect-menu labels on live edit; all
   Control-32-verified behavior (single CC out, SAVE/RECALL, persistence) unchanged.
   Note the presets filename change means a fresh file on first SAVE.
c) NEW — SYNC display: Slicer on a bus, turn SYNC (CC82 k5) ON, then Slicer SPEED (k1)
   should show named divisions (2/1, 1/1T, …) instead of 0-100. Same for SuperFilter
   RATE, TimeCtrlDly TIME, Zan-Zou TIME. (Breakpoints etid32/33/34 still unverified on
   hardware — this tests only that the mode switch finally engages.)
d) NEW — DFX in presets: change a DFX slot menu, SAVE, change it again, RECALL —
   menu position AND the bus effect-menu labels that depend on it must restore.
   Close/reopen Max and RECALL again (Control33_presets.json round-trip, now with
   45 fields per slot).

## 32: pattrstorage preset system (replaces psys-* coll-based system)
Full cutover from the Control-31 `obj-psys-*` (coll-based, 49 objects) preset system to
one built on `pattrstorage`/`pattrforward`, following the mechanism validated in
`SP404_preset_TEST_v16.maxpat`. Generated by `sp404gen/build_control32.py` (run it to
regenerate from Control-31 after any further edits to that base file); structurally
validated by `sp404gen/validate_control32.py` (confirms no dangling references, no
orphaned inlets/outlets, all 40 pattr client varnames match the subscribe list — does
NOT confirm the patch runs correctly in Max, which requires actually opening it).
Diffed against Control-31: every non-psys object and connection is byte-for-byte
identical; only `psys-*` was removed and the new `obj-pv2-*` system added on top.

**Design, and where it necessarily diverges from the v16 prototype** (v16 was a
simplified mechanism test — no MIDI, no enum display, no dynamic per-bus menus):
- 40 pattr client objects (8 params × 5 buses), not v16's 35 — the prototype never
  covered the effect-select menu's real per-bus rebuild or the bus on/off toggle (CC19).
- Bus switch retargets `pattrforward` (send Bus%d_X) off the real bus-select `obj-12`,
  additive to its existing (untouched) `obj-19`/`obj-506`-series item-rebuild wiring.
- Display refresh for the 6 CC dials + toggle feeds a REAL value into their hot inlet
  (audible, matches deleted `psys-unres` behavior) — necessary so the downstream
  enum/SYNC pipeline (`expr $i2*1000+$i1` → gate → coll) recomputes. The effect-select
  menu (`obj-18`, plain `umenu`) instead gets a silent `set $1` PLUS an explicit
  broadcast into the 5 per-bus EFX gates' inlet 1 (`obj-pv2-efx-mfan`, replicates the
  deleted `psys-mfan`) — required because `set` doesn't fire `umenu`'s outlet, and the
  enum-mode derivation only re-runs when that outlet fires.
- Recall ordering (effect-select must land before CC values) is done via explicit
  `t`-object trigger sequencing, NOT a pattrstorage "priority" attribute — that
  attribute's existence/behavior on `live.*` parameter objects couldn't be verified
  without Max, so this uses the same technique the original system already proved.
- Hardware dispatch on recall (push all 5 buses' stored state to the real unit over
  MIDI, bypassing the UI) has no v16 analogue. It uses a SEPARATE set of 40 "hw-holder"
  ints (`obj-pv2-hwh-*`, distinct from the 40 display-shadow ints `obj-pv2-sh-*`) so
  dispatching a non-viewed bus can never leak into the currently-displayed control.
  Sequencing: `recall N` → deferlow → hw-dispatch (`uzi 5`, 1-indexed, confirmed against
  the deleted `psys-uzhw`'s own wiring) completes fully for all 5 buses FIRST, THEN the
  current bus's display refresh fires LAST, so it's the authoritative final state.
- SAVE/RECALL UI and PC 0-7→preset 1-8 mirror the deleted psys objects' UI/behavior,
  but pattrstorage's native `store N`/`recall N` eliminated all the old key-arithmetic
  objects (`psys-p1/p10/keyadd-*`) — slot number is used directly, no `(preset+1)*10+bus`
  key math needed.

**⚠ HARDWARE/MAX-TEST ITEMS (Control-32, none of this could be verified without Max):**
a) Open the file in Max and check the console for errors on load (bad object args,
   missing objects, etc.) before doing anything else.
b) Turn each of the 6 dials + toggle + effect menu on BUS 1 — confirm audible MIDI out
   matches what turning them always did (this port shouldn't change live-edit behavior
   at all, only storage/recall — regression-check this first).
c) Switch buses (obj-12) with no preset involved — confirm the 6 dials/toggle/effect
   menu redraw to silence (no MIDI) and show each bus's last-set values correctly,
   including the effect menu's item LIST (not just position) rebuilding per bus.
d) SAVE to a slot, change values, switch away and back, RECALL that slot — confirm the
   UI redraws correctly for the viewed bus AND (with hardware attached) all 5 buses'
   MIDI dispatch lands on the right channels with the right values.
e) Test incoming Program Change 0-7 → recall preset 1-8.
f) `pattrstorage @savemode 1` is assumed to embed preset data in the saved .maxpat file
   (matching the old coll-@embed-1 single-file-distribution requirement) — this is a
   well-documented Max attribute value but wasn't hardware/Max-verified this session;
   confirm by saving, closing, reopening, and checking a stored preset survived.
g) Known unchanged gap, not in scope for this port: DFX slot assignments
   (obj-43/84/125/166/207) still aren't part of any preset, same as Control-31.

If Control-32 checks out in Max, it becomes the new "latest shipped" and this file's
top line should be updated accordingly; if it doesn't, Control-31 is untouched and
still fully working as a fallback.

### Bug found in first no-hardware Max test, fixed same session
User-reported symptoms (no hardware attached, watched MIDI out directly):
1. Every control (EFX menu, knobs, on/off button) sent its CC out TWICE.
2. RECALL worked, but visibly corrupted the CURRENTLY VIEWED bus's dials, even
   though the stored data for non-viewed buses (checked directly on the hidden bg
   objects) was correct.

Root cause (one bug, two symptoms): the "continuous tap" wire from each bg client
object into its shadow int (`obj-pv2-sh-*`, display) and holder int (`obj-pv2-hwh-*`,
hw-dispatch) was wired into the int object's HOT inlet 0. A Max `int` object's hot
inlet stores AND immediately re-outputs on any incoming value. Since:
  - `obj-pv2-sh-*`'s output feeds directly into the visible control's hot inlet, and
  - `obj-pv2-hwh-*`'s output feeds directly into a `ctlout`,
...every live edit (forwarded via pattrforward into the currently-targeted bg object,
which then fires because it changed) immediately echoed straight back out through
BOTH paths -- a second MIDI send on every edit (symptom 1), plus a spurious send on
whatever stale channel `obj-15` last held. During RECALL, `pattrstorage` updates all
40 bg objects in its own internal order (not bus-grouped, not otherwise controlled by
this patch), so ALL of them briefly flooded the single set of visible controls before
the deliberate post-recall refresh (which fires last, on purpose) could overwrite it
back to the correct current-bus value -- except the same hot-inlet tap kept re-firing
into the visible controls throughout that flood, racing the deliberate refresh
(symptom 2).

Fix: route the continuous tap into the int object's COLD inlet 1 instead (stores the
value silently, no output) for both `obj-pv2-sh-*` and `obj-pv2-hwh-*`. The deliberate
triggers (`sel_{param}` bang for display refresh, `hw-bussel` bang for hw-dispatch)
still target the HOT inlet 0, so they're the ONLY thing that makes these objects
output. Applied in `build_control32.py`; Control-32 regenerated and re-validated
(structural check + full non-psys diff against Control-31, still byte-identical).

**Re-tested by user in Max after the fix (2026-07-08, no hardware): confirmed working.**
Single CC-out per control action, and SAVE/RECALL now correctly refresh the
currently-viewed bus's dials without corruption.

### Persistence bug found on close/reopen, fixed same session
User closed Control-32.maxpat, got prompted to save a "presets" file, saved it,
reopened the patch — nothing could be recalled anymore.

Root cause: `pattrstorage` does NOT embed its data inside the .maxpat the way
`coll @embed 1` does (confirmed against Max's own docs, not assumed) — it always
persists to a separate companion JSON/XML file. `@savemode 1` (what was used) only
means "prompt to save that companion file when the patcher closes" — it has nothing
to do with embedding. Worse, `@autorestore 0` (copied verbatim from the v16
prototype's settings, which only cared about not sending MIDI on load) is the
attribute that makes pattrstorage automatically find and reload that companion file
on patch open — with it off, the freshly-saved file was never read back in, so
`pattrstorage` started every session completely empty regardless of what got saved.

User was asked and chose: keep pattrstorage's native companion-file model (not worth
re-engineering back to a single embedded file) but make it deterministic instead of
relying on the close-time prompt and default file-name guessing.

Fix: pinned explicit `read`/`write` messages to a fixed filename,
`Control32_presets.json` (bare filename — Max's docs confirm the patcher's own
folder is searched by default, so this travels correctly as long as the two files
stay together). `@savemode 0 @autorestore 1` — no more close-time prompt;
`write "Control32_presets.json"` fires automatically right after every SAVE-button
press (deferred one tick so the store completes first), so presets hit disk
immediately rather than depending on the user remembering to save/close, or a
dialog they might dismiss. `read "Control32_presets.json"` fires on load, after
`subscribe` completes. New objects: `obj-pv2-load-defer2`/`obj-pv2-read-msg` (load
side), `obj-pv2-save-defer`/`obj-pv2-write-msg` (save side).

**Note:** the presets file the user saved via the earlier (broken) close-prompt
went wherever that dialog defaulted to, NOT `Control32_presets.json` — it's an
orphan and can be ignored/deleted. The new mechanism will create
`Control32_presets.json` fresh, in the same folder as the .maxpat, the first time
SAVE is pressed after reopening with this fix.

User confirmed `Control32_presets.json` does get created in the right folder, but
after closing/reopening Max, RECALL for the saved preset still did nothing.

### Slot-numbering bug found on file-persistence retest, fixed same session
Root cause: `obj-pv2-slotmenu` is a plain `umenu` ("Preset 1".."Preset 8" items) --
umenus are 0-INDEXED, so selecting "Preset 1" outputs `0`, not `1`. That raw value
was fed straight into `prepend store`/`prepend recall` with no adjustment, so
pressing SAVE on "Preset 1" actually sent `store 0` to pattrstorage, not `store 1`.
Slot 0 is apparently enough of an edge case that it round-trips oddly through
pattrstorage's file format (worked live in-session, silently failed after a real
file save/reload) -- consistent with the exact symptom reported. The ORIGINAL
psys-* system had already solved this exact conversion (`psys-curpre -> +1`
before its own key math); this session's port dropped the key-math objects
correctly (pattrstorage doesn't need them) but incorrectly dropped the `+1` along
with them.

Fix: inserted `+1` (`obj-pv2-slot1-store`, `obj-pv2-slot1-rcl`) between
`slotmenu`'s raw output and the `store`/`recall` messages, so "Preset 1" now
correctly maps to pattrstorage slot 1. Also fixed a related bug this surfaced in
the PC-recall chain (`obj-pv2-pgt` section): it applied `+1` BEFORE splitting into
the display-set and recall-message branches, so both got the already-1-based
value -- meaning the on-screen slot display was off by one (PC=0 showed "Preset 2"
instead of "Preset 1") while recall itself happened to target the right slot only
by coincidence of both being consistently wrong. Restructured so the raw 0-based
PC value feeds the (0-indexed) display `set`, and `+1` is applied only on the
branch heading to `recall`.

**Re-tested by user in Max (2026-07-08, no hardware): confirmed working.**
SAVE -> close/reopen Max -> RECALL now round-trips correctly through
`Control32_presets.json`. PC 0-7 display-offset fix not separately re-tested yet
(low risk, same code path pattern as the verified button path).

### Unrelated pre-existing bug noticed during testing (not part of this port)
Max console error `expr: syntax error ? 32 : -1` traced to `obj-sync-sl-ex` /
`obj-sync-sf-ex` (Slicer SPEED / SuperFilter RATE SYNC-mode detection, added in
Control-30): `expr ($i1 >= 1) ? 32 : -1`. Confirmed byte-identical in Control-31 --
not caused by this session's port. Confirmed via Max's own forums: `expr`'s C-style
ternary `?:` operator has never actually been implemented in Max (long-standing
unfulfilled feature request), so this expression has likely silently failed to
compile since it was first added. Not fixed that session (out of scope for the
preset-system port). **FIXED in Control-33** (all four occurrences, including
tc/zz which this note originally missed — see "33" section).

Hardware-dependent items (real per-bus MIDI dispatch, PC 0-7 recall to real unit)
are still open — see "HARDWARE/MAX-TEST ITEMS" list above, items (d) hardware
half, (e).

## Version history this session
- 13: 13 enum-content fixes (Stopper RATE + 12 more, manual-verified)
- 14: = 13 (shipped copy)
- 15: BROKEN breakpoint attempt (two-hop lookup, timing bugs) — reverted
- 16: breakpoint rebuild — combined CC->text colls (enum_k* rekeyed etid*1000+CC),
      same hop-count as original: dial -> expr -> gate -> coll -> display
- 17: balance fix — 17 wet/dry params, cc2bal_k1/2/4/5 colls from 19 measured points
- 18: SX Delay TIME split from Ko-Da-Ma table (own breakpoints, etid0 knob0)
- 19: 7x dB-EQ params (-15..15) converted to discrete enum floor(CC*31/128)-15
- 20: Slicer PATTERN (32-step) + Lo-fi CUTOFF (17 freqs, 200..8000)
- 21: Chromatic PS re-layout — 5 separate params (was wrongly packed)
- 22: Chromatic PS PAN direction corrected (CC0=L50, CC127=R50)
- 23: Reverb LOW CUT (18: FLAT,20..800) + HIGH CUT (15: 630..12500,FLAT)
- 24: Hyper-Reso/Vocoder SCALE = 24 keys (C Maj..B Maj, C min..B min, sharps, shared etid23 knob3)
- 25: all "1.25k"-style freqs -> full numbers ("1250"); all int enum values -> strings
- 26: Cloud Delay PITCH — 121-step fractional (floor(CC*121/128)*0.2-12, "%.1f" format;
      known 0.2-off at CC96/112, accepted)
- 27: INPUT bus label freeze FIXED — gate obj-c3135 inlets were swapped vs working buses
- 28: DirectFX 1-5 routing FIXED — select outlets 37-41 (menu pos 38-42 = the five
      BUS1/2-exclusive effects) were unwired on all 5 slots; added 25 writer msgs
      (keys 100-104) + 50 lines. Loadbang defaults now actually reach the value objects.
- 29: k5 etid30 (Harmony HARMONY) content fixed — was still old Resonator CHORD data
      [Root, Oct, UpDn, P5, m3, m5, m7, m7oct, m0, m11]; corrected to
      [Root, P5, Oct, UpDn, UpDnP5, 3rd, 5thUp, 5thDn, 7thUp, 7thDn]. k4 etid30
      (Vocoder CHORD) was already correct since Control-28.
- 30: SYNC-dependent dual-mode (Category C) — architecture + 4 effect pairs implemented.
      21 new objects, 33 new lines. See ARCHITECTURE section for full design.
- 31: PRESET SYSTEM + MIDI control (obj-psys-* namespace, 49 objects, 97 lines):
      8 preset slots for all-5-bus FX states, per-bus shadow state with UI restore
      on bus switch, hardware recall via channel-iterated CC dispatch, incoming
      Program Change 0-7 recalls presets 1-8. See PRESET SYSTEM section.
- 32: preset system cutover to pattrstorage/pattrforward (obj-pv2-*, replaces
      psys-*). All no-hardware Max tests passed after 3 fixes. See "32" section.
- 33: 4x SYNC-detect expr ternary fix (never compiled since Control-30) + DFX
      slots 1-5 added to presets (obj-pv33-*, UI-only — DFX chain has no MIDI
      out). Presets file renamed Control33_presets.json. See "33" section.
      Both this version's user-found bugs (cross-group label scramble,
      channel-5 residue) retested and confirmed fixed 2026-07-12.
- 34: MIDI Control Input (obj-c34-*: device umenu + ctlin + per-control CC
      selector/comparator/gate for the 6 dials + EFX select + EFX on/off) +
      128 presets (was 8) with per-preset renaming via an embedded coll +
      textedit UI. Presets file renamed Control34_presets.json. See "34"
      section — this version has the least in-codebase precedent so far.

## HARDWARE-VERIFIED RULES (do not re-derive; these were measured)
1. GENERAL discrete rule: index = floor(CC * N / 128). Confirmed N=2,4,5(JUNO),6,9,14,17,18,24,31,32,37,101,121.
2. EXCEPTIONS with own measured breakpoints:
   - Back Spin LENGTH (N=5): boundaries 32,64,96,127
   - Ko-Da-Ma/Sync Delay TIME (N=16): 9,16,24,32,40,48,56,64,72,80,88,96,104,112,127 (15/15 verified)
   - SX Delay TIME (N=16, DIFFERENT!): 9,17,26,34,43,51,60,68,77,85,94,102,111,119,127
   - JUNO Chorus MODE (N=5): 26,51,77,102 (differs from formula at 2/4 boundaries)
3. Balance (wet/dry): 19 measured points (0:0, 8:6, 16:13, 24:19, 32:25, 40:31, 48:38,
   56:44, 63:49, 64:50, 65:51, 72:56, 80:63, 88:69, 96:75, 104:82, 112:88, 120:94, 127:100
   as CC:wet), interpolated between. No clean formula fits all.
4. KEY LESSON: identical value NAMES across effects do NOT imply identical CC breakpoints
   (SX Delay vs Ko-Da-Ma). Verify per-effect.
5. Display formats: freqs full numbers ("1250" not "1.25k"); PAN "L50..C..R50";
   pitch fractional "%.1f" ("0.0"); KEY list starts at A with flats for Auto Pitch/Harmony
   but Hyper-Reso SCALE uses C-start with sharps ("C Maj".."B min").

## ARCHITECTURE (post-Control-16, updated Control-30)
- enum_k0..k5 colls: keyed etid*1000+CC -> final display text (128 entries per etid)
- Signal: live.dial -> expr $i2*1000+$i1 (i1=raw CC hot, i2=mode/etid cold from
  existing mode-cold-store int) -> gate (mode>=0) -> enum coll -> display msgbox
- Balance: dial -> coll cc2bal_kN (key=CC) -> gate (mode==-2) -> display msgbox
- Numeric (-1): unchanged original scale->sprintf path
- prm_k*[idx] = [lo, hi, mode]; mode: -1 numeric, -2 balance, >=0 etid
- EFX menu obj-18 items are DYNAMICALLY rebuilt per bus (obj-20/22/24/26/28
  clear/append msgs). Saved file only shows last state — don't trust static items.
- busid_BUS12/BUS34/INPUT colls: menu position -> effect index. DFX slots use keys 100-104.

### SYNC-dependent mode switching (added Control-30)
- Current effect index stored in obj-sync-ceidx (int), tapped from c3124 outlet 0.
- k5 dial (CC82, obj-498) tapped via obj-sync-sy01 (t i i):
    outlet 1 (fires FIRST): raw CC → per-handler CC stores (obj-sync-sl-cc etc.)
    outlet 0 (fires SECOND): int→bang via obj-sync-sy03 → bangs obj-sync-ceidx → effect idx
      → obj-sync-sy04 (route 34 36 39 45) → per-effect t b b handlers
- Per-effect handler (t b b):
    outlet 1 (bang, FIRST): bangs per-handler CC store → >= 1 → expr → writes mode to
      target knob's cold-store int (c3041/c3092/c3024 for k1/k4/k0)
    outlet 0 (bang, SECOND): bangs target knob's CC-value int (c3039/c3090/c3022)
      → t i b chain → cold-store outputs new mode → expr cold inlet updated;
      display refreshes on next knob movement.
- Effect/knob/etid mapping:
    Slicer (34)     SPEED k1  SYNC=k5  synced=etid32  unsynced=-1
    SuperFilter (36) RATE k4  SYNC=k5  synced=etid32  unsynced=-1
    TimeCtrlDly (39) TIME k0  SYNC=k5  synced=etid33  unsynced=-1
    Zan-Zou (45)    TIME k0  SYNC=k5  synced=etid34  unsynced=-1
- etid32 (k1,k4): 22-step synced divisions — 2/1,1/1T,1/1..1/64,1/128
    ⚠ BREAKPOINTS UNVERIFIED (floor(CC*22/128) assumed). Verify on hardware for each effect.
- etid33 (k0): TimeCtrlDly TIME synced — 16-step, same names as etid2.
    ⚠ BREAKPOINTS UNVERIFIED. May differ from Ko-Da-Ma style (per lesson #4).
- etid34 (k0): Zan-Zou TIME synced — 16-step, same names as etid33.
    ⚠ BREAKPOINTS UNVERIFIED. Separate etid from etid33 for independent hw calibration.
- LIMITATION: mode resets to unsynced (-1) if user switches effect and returns (prm_k
  re-reads -1 on effect select). SYNC state not tracked persistently across effect switches.
- DJFX Delay (10) SKIPPED: has etid5 on BOTH k2 and k5 — cannot identify SYNC knob
  without hardware. Needs: confirm which knob is SYNC, which are TIME/LENGTH, verify
  breakpoints for each. Then add DJFX handler using same pattern.

## Effect indices
  0=303VinylSim 1=404VinylSim 2=AutoPitch 3=BackSpin 4=CassetteSim
  5=Chorus 6=ChromaticPS 7=CloudDelay 8=Compressor 9=Crusher 10=DJFXDelay 11=DJFXLooper
  12=Distortion 13=Downer 14=Equalizer 15=Filter+Drive 16=Flanger 17=GtAmpSim 18=Ha-Dou
  19=Harmony 20=Hyper-Reso 21=Isolator 22=JUNOChorus 23=Ko-Da-Ma 24=Lo-fi 25=Overdrive
  26=Phaser 27=Resonator 28=Reverb 29=RingMod 30=SBF 31=SXDelay 32=SXReverb 33=Scatter
  34=Slicer 35=Stopper 36=SuperFilter 37=SyncDelay 38=TapeEcho 39=TimeCtrlDly 40=To-Gu-Ro
  41=Tremolo/Pan 42=Vocoder 43=Wah 44=WrmSaturator 45=Zan-Zou

## PRESET SYSTEM (added Control-31, obj-psys-* namespace)
Deviation from S-1 template: uses embedded colls (not pattrstorage) — integrates with
the existing coll pipeline, keeps single-file distribution, presets saved with patch.

- ROW FORMAT (deliberately REVERSED): [v82 v81 v80 v18 v17 v16 onoff menupos]
  Reason: unpack fires outlets right-to-left, so menupos (effect select) fires FIRST —
  required both for MIDI order (CC83 before knob CCs) and so prm ranges/modes are set
  before dial values arrive. onoff stored raw 0/1 (× 127 only at ctlout dispatch).
- coll busstate (keys 1-5 = bus): live shadow of each bus's UI state. Written
  continuously: dials/toggle/menu → pak 8 → t b l → pack 0 l (key cold via
  prepend set, from bus idx + 1) → busstate. Key updated BEFORE restore on bus switch.
- coll presets @embed 1 (keys P*10+b, P=1-8, b=1-5): 40 rows, saved with the patch.
- BUS SWITCH RESTORE: obj-12 → psys-curbus int → psys-busres-t (t i i i, fires r→l):
  (2) +1 → pack key (via t i i → prepend set) AND → obj-15 channel
  (1) msg [1] → bsdemux control  (0) bus key → busstate query
  → gate 2 1 demux out0 → unpack → menupos: silent [set $1] to obj-18 + injection
  into all 5 EFX gates' inlet 1 (only active bus's gate is open) → full label/prm/
  SYNC machinery fires with NO MIDI; then onoff + 6 dials restored AUDIBLY (same CC
  values hardware already has = no state change, but displays refresh correctly).
- COLL OUTPUT DEMUX: busstate and presets each have a gate 2 1 on their outlet,
  control set by msg [1]/[2] immediately before each query (synchronous, race-free):
  busstate: 1=UI restore, 2=save copy. presets: 1=hw dispatch, 2=recall copy.
- SAVE: t b b → (r) bang curpre → +1 → *10 → cold key adders; (l) uzi 5 → per bus:
  set demux 2, query busstate[b], key=(P+1)*10+b → zl join → presets store.
- RECALL: t b b b b (r→l): (3) bang curpre/base; (2) uzi 5 hw dispatch — per bus:
  demux 1, channel b → obj-15, presets[key] → unpack → CC83 (obj-30), CC19 via
  * 127 (obj-456), CC16/17/18/80/81/82 (obj-459/467/475/483/491/499), all inlet 0
  (bypasses UI); (1) uzi 5 copy presets→busstate; (0) bang curbus → full UI +
  channel restore for the currently-viewed bus.
- MIDI CONTROL: bare pgmin (all ports/channels) → split 0 7 → t i i i:
  set preset umenu silently, update curpre, trigger RECALL. PC 0-7 = presets 1-8.
- loadbang init: pack key = set 1 (BUS 1), preset umenu = set 0. NO MIDI sent on load.
- ⚠ HARDWARE-TEST ITEMS (Control-31):
  a) live.dial/live.toggle/umenu response to bare int input and 'set' message —
     verify silent vs audible behavior matches design.
  b) Recall dispatch sends all 5 buses back-to-back with no inter-bus delay —
     if hardware drops/mangles messages, insert pipe-based pacing between buses.
  c) Effect re-select on recall (CC83 with same value) — check if hardware re-inits
     the effect audibly; if so, add change-detection before CC83 send.
  d) Known gap: recall does NOT restore DFX slot assignments (obj-43/84/125/166/207)
     — DFX1-5 menu contents are separate global state, not per-bus. Add to presets
     as a follow-up if needed (5 extra fields, own CC dispatch TBD from DFX wiring).

## REMAINING OPEN ITEMS
(updated for Control-33: expr-ternary fix DONE, retested confirmed; DFX-slots-in-presets
DONE, retested confirmed. Updated for Control-34: MIDI Control Input DONE, 128 presets +
renaming DONE — both pending Max testing, see "34" MAX-TEST ITEMS, and see "34" section's
⚠ UNVERIFIED notes for the specific mechanisms most likely to need a fix-cycle.)
1. DJFX Delay SYNC: need hardware confirmation of which knob is SYNC toggle and which
   are TIME/LENGTH; then verify breakpoints before adding handler.
2. Tremolo/Pan RATE (k1) + Wah RATE (k1): show "1.000-0.010" when SYNC=ON (continuous
   range, not named divisions). Currently numeric -1, which displays linear 0-100.
   Decision needed: accept as-is or implement inverted continuous display.
3. etid32/33/34 breakpoints: all flagged ⚠ above — verify on hardware and adjust
   coll entry ranges (same method as SX Delay TIME in Control-18).
4. Tremolo/Pan WAVE (k3 etid21): has 7 entries including RND. Manual lists only 6 named
   waveforms. Needs hardware check: is RND a real 7th option or an error?

## PRESET SYSTEM — VERIFIED ARCHITECTURE (test patch v16, July 2026)

### Core design (pattrforward + pattrstorage, user's methodology)
- 35 real parameter-enabled bg objects: live.dial (varname "BusN_ccXX") + live.menu
  (varname "BusN_efx") for each of 5 buses. These are pattrstorage's tracked clients.
- 7 visible controls (6 number boxes + 1 live.menu): NO varname, NOT pattr-tracked.
- autopattr object present (bare, unconnected) — REQUIRED for @subscribemode 1 to work.
- pattrstorage @subscribemode 1 + explicit 'subscribe <name1>..<name35>' sent via
  loadbang -> deferlow -> subscribe_msg -> pstore (registration before any bindto).
- After subscribe, a second deferlow fires busmenu '0' to trigger initial bus1 binding.

### Bus switch mechanism
- busmenu int outlet -> +1 -> bus_t (t b i):
  - out1 (int, FIRST): -> 7x sprintf 'send Bus%d_X' -> pattrforward retargets (write)
                        -> busN int (HOT inlet, stores AND outputs) -> 7x select 1-5
                        -> bang matching shadow int -> shadow outputs -> prepend set
                        -> visible control (silent display update)
  - out0 (bang, SECOND): unconnected (display handled by busN fan above)
- 35 shadow ints (one per bg object): tap bg object outputs, hold last value.
  select 1-5 bangs the correct one on bus switch.

### Write direction (user moves visible control)
- visible -> pattrforward (target = current bg varname via 'send BusN_X' message)
- pattrforward forwards value directly to bg live.dial/live.menu inlet
- pattrstorage tracks bg objects automatically (parameter_enable=1, proper attributes)

### Preset SAVE/RECALL
- Slot umenu (1-8) -> prepend store -> message right inlet (cold set);
  SAVE btn -> t b b: out1(first) bangs slotmenu -> fires prepend -> sets msg;
  out0(second) bangs store_msg -> 'store N' -> pattrstorage. (prepend store idiom)
- Same pattern for recall with prepend recall.
- After recall: pstore out0 -> deferlow -> t b -> bang busN -> busN outputs current
  bus -> 7 select -> shadows -> visible (display refresh after pattrstorage restores)

### CRITICAL LESSONS (hard-won)
1. pattrforward retargets via 'send <name>' message (NOT bare symbol). Use
   'sprintf send Bus%d_X' not 'sprintf symout Bus%d_X'.
2. pattr 'bindto' masks the target from pattrstorage — never use bindto on objects
   that pattrstorage must track.
3. autopattr (bare, no connections) MUST be present for @subscribemode 1 to work.
4. pattrstorage 'Client Objects' window shows LIVE values, not saved slots.
   Use 'storagewindow' message to pattrstorage to see per-slot saved data.
5. Visible controls must have NO varname — if they have a varname, pattrforward
   loops back to them instead of targeting the bg object.
6. 'set $1' on an int stores silently (no output). Feed bus number to int HOT
   inlet (inlet 0 with plain int) so it stores AND outputs immediately.
7. Subscribe list names must match bg object varnames EXACTLY (case-sensitive).
8. Use shadow ints (one per bg object) to hold last values — bg live.dial only
   outputs on change, not on bang. Shadow ints can be banged to re-output.
9. SAVE/RECALL button: use t b b — first bang fires slotmenu (sets prepend cold),
   second bang fires the store/recall message. Without first bang, saves to slot 0.
10. After pattrstorage recall, use deferlow before triggering display refresh —
    gives pattrstorage time to restore all 35 values before display reads them.
